% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icc_curves.R
\name{icc_crossings}
\alias{icc_crossings}
\title{Pairwise category-crossing points of an \code{icc_curves()} object}
\usage{
icc_crossings(x)
}
\arguments{
\item{x}{A \code{bilatr_icc_curves} object, as returned by \code{\link[=icc_curves]{icc_curves()}}.}
}
\value{
A tibble, one row per category pair: \code{index_a}, \code{index_b},
\code{label_a}, \code{label_b}, \code{crossing_median}, \code{crossing_lower},
\code{crossing_upper}, \code{in_range} (whether \code{crossing_median} falls inside
\code{x}'s \code{theta_range}).
}
\description{
The theta at which two categories' response curves cross is available
in closed form: \code{theta = (mu_j - mu_k) / (alpha_j - alpha_k)}, since
\code{p_j(theta) == p_k(theta)} iff \code{eta_j(theta) == eta_k(theta)} (softmax
is strictly increasing, so equal probabilities need equal linear
predictors) iff \code{alpha_j * theta - mu_j == alpha_k * theta - mu_k}.
Computed per posterior draw (reusing \code{x}'s stored \code{alpha}/
\code{mu_intercept} draws -- no re-read of \code{fit}) and summarised to a
median and posterior interval; undefined (\code{NA}) for draws where
\code{alpha_j == alpha_k} exactly.
}
\details{
This ties back to \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}'s merge diagnostics: a
pair whose crossing falls outside the observed \code{theta_range}
(\code{in_range = FALSE}) is a pair the scale never actually distinguishes
over the range dyads occupy, regardless of how different their
discriminations look in the abstract.
}
