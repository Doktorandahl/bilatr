% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_check.R
\name{.rmultinom_rows}
\alias{.rmultinom_rows}
\title{Vectorized multinomial sampling with a FIXED size and per-row
probabilities}
\usage{
.rmultinom_rows(n, p_mat)
}
\arguments{
\item{n}{A single non-negative integer, the multinomial size (assumed
identical across rows).}

\item{p_mat}{An \verb{n_draws x A} matrix of row-stochastic probabilities.}
}
\value{
An \verb{n_draws x A} integer matrix of multinomial draws, each row
summing to \code{n}.
}
\description{
\code{n} (the multinomial size) is a single scalar shared across every row
of \code{p_mat} -- true here because the Dirichlet-multinomial replicate is
always simulated at the OBSERVED \code{n_dt} (see the module header), which
does not vary by posterior draw, only the concentration/probability
does. This lets the whole \code{nrow(p_mat)}-row multinomial draw be
vectorized as \code{A - 1} sequential \code{\link[stats:Binomial]{stats::rbinom()}} calls (stick-
breaking) instead of \code{nrow(p_mat)} separate \code{\link[stats:Multinom]{stats::rmultinom()}} calls
-- the difference that keeps \code{\link[=check_compositional_residuals]{check_compositional_residuals()}}'s
per-dyad-period simulation loop fast at the scale 1d describes (up to
~1500 sampled dyads x ~35 periods).
}
\keyword{internal}
