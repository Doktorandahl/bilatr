% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{eventrootcode4_rootcodes}
\alias{eventrootcode4_rootcodes}
\title{Underlying CAMEO root/event codes for an \code{EventRootCode4} value}
\usage{
eventrootcode4_rootcodes(class)
}
\arguments{
\item{class}{Integer vector of \code{EventRootCode4} values, as returned
by \code{\link[=assign_eventrootcode4]{assign_eventrootcode4()}}.}
}
\value{
Character vector of comma-separated CAMEO root/event codes,
\code{NA} for values outside 1-23.
}
\description{
For most \code{EventRootCode4} classes this is just the single two-digit
CAMEO root code the class was built from. Where a class merges
several roots (e.g. "Engage in diplomatic cooperation", which picks
up \code{018}/\code{019} from root 01), splits a root into finer sub-codes
(e.g. "Consult: meet, discuss, or visit"), or relocates an individual
code out of its root (e.g. "Reject", which picks up \code{016} from root
01), this lists every code that feeds into it, comma-separated in the
order given in \code{\link[=assign_eventrootcode4]{assign_eventrootcode4()}}'s docs.
}
\keyword{internal}
