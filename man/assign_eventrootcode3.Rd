% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{assign_eventrootcode3}
\alias{assign_eventrootcode3}
\title{Regroup a CAMEO event code into the bilatr "EventRootCode3" scheme}
\usage{
assign_eventrootcode3(code)
}
\arguments{
\item{code}{Character or numeric vector of CAMEO event codes (e.g.
\code{"0211"}, \code{"19"}).}
}
\value{
Integer vector of \code{EventRootCode3} values (1-19).
}
\description{
Deprecated: retained internally for backward compatibility; no longer
part of \link{cameo_lookup}. Superseded by \code{ModifiedRootCode}; see
\code{\link[=assign_modified_root_code]{assign_modified_root_code()}}.
}
\details{
\code{EventRootCode3} is a further refinement of \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}},
motivated by wanting a few of its coarser groupings split back out
while relocating individual codes that behave more like a different
category than their root suggests:

\itemize{
\item \code{016} ("Deny responsibility") moves out of root 01 into the
Reject class (with root 12).
\item \code{018} ("Make empathetic comment") and \code{019} ("Express accord")
move out of root 01 into the diplomatic-cooperation class (with
root 05).
\item \code{EventRootCode2}'s \code{"10"} (roots 09 + 10, "Investigate or
demand") is split back into Investigate (root 09) and Demand
(root 10).
\item \code{EventRootCode2}'s \code{"13"} (roots 13 + 14 + 15, "Threaten,
protest, or exhibit force posture") is split so that root 14
("Protest") becomes its own class, while roots 13 and 15
("Threaten" and "Exhibit force posture") stay merged.
\item Every other root maps as in \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}}.
}

Classes are numbered 1-19 in ascending root-code order; see
\code{\link[=eventrootcode3_name]{eventrootcode3_name()}} for labels and
\code{\link[=eventrootcode3_rootcodes]{eventrootcode3_rootcodes()}} for the underlying CAMEO root/event
codes each class draws from.
}
\keyword{internal}
