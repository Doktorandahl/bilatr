% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.read_diagnostics_summary_from_csv}
\alias{.read_diagnostics_summary_from_csv}
\title{Build the tier-classified summary tibble directly from raw CmdStan CSVs}
\usage{
.read_diagnostics_summary_from_csv(
  csv_files,
  tiers,
  max_memory_mb,
  chunk_size,
  parallel,
  n_workers,
  max_memory_mb_missing
)
}
\arguments{
\item{csv_files}{Character vector of CmdStan CSV file paths.}

\item{tiers, max_memory_mb, chunk_size, parallel, n_workers}{See
\code{\link[=diagnose_convergence]{diagnose_convergence()}}.}

\item{max_memory_mb_missing}{Whether the caller left \code{max_memory_mb}
at its default (via \code{missing()} in \code{\link[=diagnose_convergence]{diagnose_convergence()}}) --
gates the one-time "this is a default, not a calibrated value"
message.}
}
\value{
A tibble in the same shape \code{\link[=diagnose_convergence]{diagnose_convergence()}}'s
in-memory branch produces: \code{\link[posterior:draws_summary]{posterior::summarise_draws()}} columns
left-joined with \link{.classify_bilatr_tier}'s \code{tier}/\code{dyad_id}/
\code{time_index}.
}
\description{
The CSV-path counterpart of the in-memory branch in
\code{\link[=diagnose_convergence]{diagnose_convergence()}}: reads only Tier 1/2 variables in one small
read (cheap, as today), and Tier 3 variables (typically, by far, the
most numerous of the three tiers) in memory-bounded chunks via
\link{.summarise_tier3_chunked}. Never materializes the full multi-chain
draws array in memory, unlike the in-memory branch, which necessarily
receives an already-fully-read \code{fit}.
}
\keyword{internal}
