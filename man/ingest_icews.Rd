% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-ingestion.R
\name{ingest_icews}
\alias{ingest_icews}
\title{Read and filter ICEWS event export zips to relevant dyadic events}
\usage{
ingest_icews(files, relevant_sectors = relevant_actors_icews())
}
\arguments{
\item{files}{Character vector of paths to ICEWS event export zip
files.}

\item{relevant_sectors}{Character vector of sector-name substrings
identifying relevant actors. Defaults to \code{\link[=relevant_actors_icews]{relevant_actors_icews()}}.}
}
\value{
A data frame of event-level records with \code{Actor1CountryCode},
\code{Actor2CountryCode}, \code{SQLDATE}, \code{QuadClass}, \code{PentaClass},
\code{PentaClass_modified}, and the original ICEWS columns.
}
\description{
Reads one or more ICEWS event export zips, filters to cross-country
dyadic events where both actors are state/security sectors (per
\code{\link[=relevant_actors_icews]{relevant_actors_icews()}}), and recodes CAMEO codes to QuadClass/
PentaClass via the package's own \link{cameo_lookup} (matching a code that
initially fails to match after a first round of leading-zero
normalization is retried with a second round, since ICEWS's numeric
CAMEO code field can drop more than one leading zero for some codes).
Output columns are renamed to match \code{\link[=extract_all_relevant_gdelt]{extract_all_relevant_gdelt()}}'s
schema (\code{Actor1CountryCode}, \code{Actor2CountryCode}, \code{SQLDATE}), so both
data sources can be aggregated with the same
\code{\link[=grouped_events_to_dyad_period]{grouped_events_to_dyad_period()}} / \code{\link[=assemble_stan_data]{assemble_stan_data()}} pipeline.
}
\examples{
\dontrun{
future::plan(future::multisession, workers = 8)
events <- ingest_icews(list.files("data/icews", pattern = "zip$", full.names = TRUE))
}
}
