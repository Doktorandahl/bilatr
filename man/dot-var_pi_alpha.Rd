% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category_merges.R
\name{.var_pi_alpha}
\alias{.var_pi_alpha}
\title{Per-draw \code{Var_pi(alpha)} and its per-category decomposition}
\usage{
.var_pi_alpha(alpha_mat, shares)
}
\value{
A list: \code{alpha_bar} (length \code{n_draws}), \code{var_pi} (length
\code{n_draws}), \code{info_share} (\verb{n_draws x A}, each row summing to 1).
}
\description{
\verb{Var_pi(alpha) = sum_k pi_k (alpha_k - alpha_bar)^2}, the Fisher
information about \code{theta} from one unit-weight multinomial observation
(see \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}). \code{shares} may be a fixed length-\code{A}
vector (recycled across draws) or an \verb{n_draws x A} matrix (one share
vector per draw, e.g. \code{shares = "theta0"}).
}
\keyword{internal}
