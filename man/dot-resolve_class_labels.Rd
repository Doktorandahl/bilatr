% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{.resolve_class_labels}
\alias{.resolve_class_labels}
\title{Resolve a \code{class_labels} argument to one label per event class, in order}
\usage{
.resolve_class_labels(class_labels, event_classes, stan_data = NULL)
}
\arguments{
\item{class_labels}{\code{NULL}, a character vector named by event class
(must cover every value in \code{event_classes}; errors listing what's
missing otherwise), or an unnamed character vector the same length
as \code{event_classes}, in \code{event_classes} order (trusted as-is, since
there is no class value to key an unnamed vector by).}

\item{event_classes}{Character vector of event classes, in the order
the caller's output rows use.}

\item{stan_data}{Optional. When \code{class_labels} is \code{NULL} and this is
supplied, defaults to \code{event_class_labels(stan_data)} (see
\code{\link[=event_class_labels]{event_class_labels()}}). Falls back to \code{event_classes} themselves
when \code{stan_data} is \code{NULL} (possible for \code{\link[=icc_curves]{icc_curves()}}), or when it
lacks a usable \code{"event_classes"} attribute (e.g. a hand-built
\code{stan_data} list in a test or a pre-0.9.0 \code{stan_data.rds}) --
\code{class_labels}/\code{event_classes} are the caller-supplied fallback for
exactly that case, so a \code{stan_data} that can't answer
\code{\link[=event_class_labels]{event_class_labels()}} on its own is not a hard error here.}
}
\value{
Character vector, \code{length(event_classes)}, in \code{event_classes}
order.
}
\description{
Shared by \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}, \code{\link[=extract_gamma]{extract_gamma()}},
\code{\link[=icc_curves]{icc_curves()}}, and \code{\link[=check_compositional_residuals]{check_compositional_residuals()}} (0.10.1,
replacing each function's own \code{class_label_fn(action_index)} handling).
Always looks labels up by event class VALUE, never by position, so a
\code{class_labels} vector (or the default from \code{\link[=event_class_labels]{event_class_labels()}})
need not share \code{event_classes}' order.
}
\keyword{internal}
