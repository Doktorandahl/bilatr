% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_data.R
\name{compute_default_weights}
\alias{compute_default_weights}
\title{Compute inverse-frequency dyad/period/action reweighting vectors}
\usage{
compute_default_weights(
  events_array,
  components = c("dyad", "period", "action")
)
}
\arguments{
\item{events_array}{A \verb{D x T x A} array of event counts.}

\item{components}{Character vector, any non-empty subset of \code{c("dyad", "period", "action")}, naming which reweighting scheme(s) to compute.
Defaults to all three.}
}
\value{
A named list containing \code{dyad_weight} (length D), \code{period_weight}
(length T), and/or \code{action_weight} (length A) — whichever were
requested in \code{components}. Components not requested are simply absent
from the returned list, not present as \code{NULL}.
}
\description{
Replicates the informal reweighting scheme used in earlier iterations of
this model: dyads and periods with very high total event counts are
downweighted (via \code{1 / log1p(total)}, normalized to mean 1), and action
types that occur rarely are upweighted the same way, so a handful of
high-volume dyad-periods or common action types don't dominate the
Dirichlet-multinomial likelihood. Only the reweighting schemes named in
\code{components} are computed.
}
\keyword{internal}
