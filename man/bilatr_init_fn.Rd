% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{bilatr_init_fn}
\alias{bilatr_init_fn}
\title{Build an initial-value generator matching a model's parameterization}
\usage{
bilatr_init_fn(stan_data, stan_model = .BILATR_DEFAULT_MODEL)
}
\arguments{
\item{stan_data}{A Stan data list as returned by \code{\link[=assemble_stan_data]{assemble_stan_data()}}.}

\item{stan_model}{Name registered in \code{.bilatr_stan_models}.}
}
\value{
A zero-argument function suitable for \code{cmdstanr}'s \code{init}
argument.
}
\description{
Both registered models share a non-centered \code{process_noise} hierarchy
(\code{log_process_noise_raw}) and a freely-estimated \code{alpha} (via
\code{alpha_raw}, length \code{A - 1}). \code{stan_model == "stable"} (the default,
used by the exported fitters) additionally initializes the
per-dyad-constant \code{phi}; \code{"phi_logn"} initializes
\code{log_phi0_raw}/\code{beta_logn} instead of \code{phi} (which isn't a free
parameter in that variant).
}
\keyword{internal}
