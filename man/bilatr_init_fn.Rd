% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{bilatr_init_fn}
\alias{bilatr_init_fn}
\title{Build an initial-value generator matching a model's parameterization}
\usage{
bilatr_init_fn(stan_data, stan_model = .BILATR_DEFAULT_MODEL)
}
\arguments{
\item{stan_data}{A Stan data list as returned by \code{\link[=assemble_stan_data]{assemble_stan_data()}}.}

\item{stan_model}{Name registered in \code{.bilatr_stan_models}.}
}
\value{
A zero-argument function suitable for \code{cmdstanr}'s \code{init}
argument.
}
\description{
\code{stan_model == "stable"} (the default, used by the exported fitters)
initializes the per-dyad-constant \code{phi}. Other registered models may
need different/additional init fields for parameters not present in
the stable model; \code{"phi_logn"} initializes \code{log_phi0_raw}/\code{beta_logn}
instead of \code{phi} (which isn't a free parameter in that variant).
}
\keyword{internal}
