% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icc_curves.R
\name{.resolve_icc_theta_range}
\alias{.resolve_icc_theta_range}
\title{Resolve the theta grid range for \code{\link[=icc_curves]{icc_curves()}}, cheapest path first}
\usage{
.resolve_icc_theta_range(
  fit,
  stan_data,
  theta_range,
  theta_summary,
  quantile_probs,
  n_dyads_fallback,
  seed
)
}
\description{
Never reads all of \code{theta}: an explicit \code{theta_range} is used as-is;
otherwise a \code{theta_summary} tibble (e.g. \code{\link[=extract_theta]{extract_theta()}}'s output,
which is saved to disk per-spec as \verb{<id>_theta.rds} in this project's
own runscripts, so this is normally a free, already-in-memory path)
has quantiles of its \code{mean} column taken; only as a last resort is a
draw-and-dyad subsample read directly off \code{fit}, with a \code{message()}
noting that fallback was used.
}
\keyword{internal}
