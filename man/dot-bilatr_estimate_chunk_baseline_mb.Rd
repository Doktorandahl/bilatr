% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.bilatr_estimate_chunk_baseline_mb}
\alias{.bilatr_estimate_chunk_baseline_mb}
\title{Measure the caller's own process memory footprint, for use as the
memory model's baseline term}
\usage{
.bilatr_estimate_chunk_baseline_mb()
}
\value{
Estimated baseline memory in MB.
}
\description{
Replaces a hardcoded assumption (\link{.BILATR_CHUNK_BASELINE_MB}) with an
actual reading of the current process: on Linux, \code{VmRSS} from
\verb{/proc/self/status} (the same per-process resident-memory figure the
rest of this package's benchmarking uses); everywhere else, \code{sum(gc()[ , "(Mb)"])} (Ncells + Vcells currently used) as a portable fallback --
cruder (R's own view of its heap, not the OS's view of the whole
process, so it can miss non-R allocations made by loaded C libraries)
but available anywhere. Whichever succeeds is floored at
\link{.BILATR_CHUNK_BASELINE_MB} so a session that happens to measure
lighter than that (e.g. right after a \code{gc()}) doesn't understate the
margin the constant was chosen to provide, and a measurement that
fails outright (unreadable \verb{/proc}, \code{gc()} erroring) falls back to it
entirely rather than propagating \code{NA} into the memory model.
}
\keyword{internal}
