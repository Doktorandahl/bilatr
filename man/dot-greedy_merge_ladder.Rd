% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category_merges.R
\name{.greedy_merge_ladder}
\alias{.greedy_merge_ladder}
\title{Greedy agglomerative merge ladder, ranked by posterior-mean
\code{pct_info_lost} at each step (see \code{\link[=diagnose_category_merges]{diagnose_category_merges()}} for why
the ranking uses the mean while every reported loss stays a full
per-draw distribution).}
\usage{
.greedy_merge_ladder(alpha_mat, shares, labels, probs)
}
\value{
A tibble, one row per step: \code{step}, \code{merged_a}, \code{merged_b}
(labels), \code{loss_mean}, \code{loss_lower}, \code{loss_upper} (posterior
mean/interval of that step's \code{pct_info_lost}), \code{cumulative_loss_mean},
\code{n_remaining}.
}
\description{
Greedy agglomerative merge ladder, ranked by posterior-mean
\code{pct_info_lost} at each step (see \code{\link[=diagnose_category_merges]{diagnose_category_merges()}} for why
the ranking uses the mean while every reported loss stays a full
per-draw distribution).
}
\keyword{internal}
