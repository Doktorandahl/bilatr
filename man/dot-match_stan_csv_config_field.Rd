% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_csv_read.R
\name{.match_stan_csv_config_field}
\alias{.match_stan_csv_config_field}
\title{Extract one \code{key = value} config field from a CmdStan CSV comment line}
\usage{
.match_stan_csv_config_field(line, key)
}
\arguments{
\item{line}{A single \verb{#}-prefixed line.}

\item{key}{The config key to match, e.g. \code{"num_samples"}.}
}
\value{
The matched value as a string (e.g. \code{"1000"}, \code{"false"}), or
\code{NA_character_} if \code{line} doesn't match \code{key}.
}
\description{
Config lines look like \verb{#     num_samples = 1000 (Default)} or
\verb{#     save_warmup = false (Default)}; keys are indented by nesting
level, so the same key name can in principle appear more than once at
different levels (not observed in practice for the five keys this
package parses, but guarded against anyway -- see
\code{\link[=.scan_stan_csv_header_and_skip]{.scan_stan_csv_header_and_skip()}}'s "first match wins" handling).
}
\keyword{internal}
