% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\docType{data}
\name{.BILATR_CHUNK_ARRAY_FACTOR}
\alias{.BILATR_CHUNK_ARRAY_FACTOR}
\title{Memory-model constant: the preallocated result array, the fread parse
buffer, and the one chain matrix in flight, combined}
\format{
An object of class \code{numeric} of length 1.
}
\usage{
.BILATR_CHUNK_ARRAY_FACTOR
}
\description{
See \code{\link[=.bilatr_chunk_overhead_multiplier]{.bilatr_chunk_overhead_multiplier()}} for how this and the other
\verb{.BILATR_CHUNK_*_FACTOR} constants combine into the \code{k} multiplier
\code{\link[=.compute_chunk_size]{.compute_chunk_size()}}/\code{\link[=.estimate_diagnostics_memory_mb]{.estimate_diagnostics_memory_mb()}} apply to a
chunk's raw byte count (\code{raw_mb = n_draws * n_chains * chunk_size * 8 / 1e6}). Re-derived (2026-09, \code{dev/bench_memory.R}, R 4.5.2, macOS)
against the array-native flip (no more \code{as_draws_df()} round-trip --
see \code{NEWS.md}), from two measurements at \code{n_cores = 1}, no flip: a
single-chain, 60,000-Tier-3-column, 300-draw synthetic CSV
(\code{raw_mb = 144}, \code{file_mb} ~= 202) measured 1002 MB peak RSS, and the
same shape at 2 chains (\code{raw_mb = 288}) measured 1409 MB. Subtracting
\link{.BILATR_CHUNK_BASELINE_MB} (measured separately) and \code{file_mb} from
each and solving the two-chain-count system for \code{ARRAY + PER_CHAIN / n_chains} gives \verb{ARRAY ~= 2.8}, \verb{PER_CHAIN ~= 1.6};
rounded up here for margin. Read alone (before Step 1's flip fix)
measured close to the old estimate (the read chain itself is
unchanged by that fix), so this jump from the old \code{ARRAY = 1} is
\code{summarise_draws()}'s own working set (rank-normalisation, split-Rhat,
bulk/tail ESS all build same-shaped intermediate arrays), which the
pre-refactor model never isolated from the read.
}
\keyword{internal}
