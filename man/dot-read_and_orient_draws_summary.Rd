% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_and_extract.R
\name{.read_and_orient_draws_summary}
\alias{.read_and_orient_draws_summary}
\title{Read a variable set in one pass, determining sign orientation from
\code{alpha[1]} if it's needed but wasn't already among \code{vars}}
\usage{
.read_and_orient_draws_summary(prepared, vars, flip_vars, n_cores = 1L)
}
\arguments{
\item{prepared}{Output of \link{.prepare_fast_csv_read}.}

\item{vars}{Character vector of variables to read (Tier 1/2, or
Tier 1/2 + Tier 3 together).}

\item{flip_vars}{Output of \code{\link[=.bilatr_flip_variables]{.bilatr_flip_variables()}} for the model
being read.}

\item{n_cores}{See \code{\link[=.chunked_summarise_csv]{.chunked_summarise_csv()}}.}
}
\value{
A list with \code{summ} (summary tibble) and \code{flip} (logical).
}
\description{
Shared by \code{\link[=diagnose_and_extract_bilatr]{diagnose_and_extract_bilatr()}}'s Tier 1/2-only and
Tier-1/2-plus-Tier-3-folded-together cases (see that function's
Details): reads \code{vars}, prepending \code{"alpha[1]"} first if \code{flip_vars}
is non-empty and \code{vars} doesn't already include it (tier 1 wasn't
requested), determines \code{flip} from its median sign, negates whichever
of \code{vars}' own columns \code{\link[=.bilatr_match_draws_columns]{.bilatr_match_draws_columns()}} matches
\code{flip_vars}, summarises, and drops the injected \code{alpha[1]} row
afterward so it never surfaces as a silently-incomplete alpha output.
}
\keyword{internal}
