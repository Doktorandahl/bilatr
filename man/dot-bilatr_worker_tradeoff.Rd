% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.bilatr_worker_tradeoff}
\alias{.bilatr_worker_tradeoff}
\title{Report the wall-time/core-hour trade-off across candidate \code{n_workers}}
\usage{
.bilatr_worker_tradeoff(
  n_vars,
  n_draws,
  n_chains,
  file_mb,
  max_memory_mb,
  worker_levels = c(1, 2, 4, 8, 16, 24),
  read_seconds,
  summarise_seconds_per_variable = 0.0026
)
}
\arguments{
\item{n_vars}{Number of Tier 3 variables the sweep will cover.}

\item{n_draws, n_chains, file_mb}{From \link{.prepare_fast_csv_read}.}

\item{max_memory_mb}{See \code{\link[=diagnose_convergence]{diagnose_convergence()}}.}

\item{worker_levels}{Candidate \code{n_workers} values to evaluate. Defaults
to \code{c(1, 2, 4, 8, 16, 24)}; the
\link{.BILATR_CHUNK_CORES_PER_CORE_FACTOR}/\link{.BILATR_CHUNK_CORES_STEP_FACTOR}
memory-model constants this helper's peak-MB estimate depends on are
only measured up to \code{n_cores = 8}, so treat levels above that as an
extrapolation (see those constants' docs).}

\item{read_seconds}{Wall-time (seconds) for ONE chunk's read -- a full
parse of the largest chain file, roughly independent of chunk size
(see \link{.fast_read_post_warmup_draws}). No default: this is the one
input this helper cannot estimate on its own, and the whole
wall-time column is only as good as it is. Take it from your own
job's logs (a \code{diagnose_convergence()}/\code{extract_theta()} run against
the same files reports its own read time), not a guess.}

\item{summarise_seconds_per_variable}{Wall-time (seconds) for the
CPU-bound summary step (\code{\link[posterior:draws_summary]{posterior::summarise_draws()}}'s
rank-normalised Rhat/bulk-tail-ESS), per Tier 3 variable, on ONE
core. Defaults to \code{0.0026} (~2.6 ms/variable), measured against this
package's own Tier 3 sweep (\code{dev/bench_memory.R}, 2026-09); scales
with however expensive computing those statistics is for your
posterior (number of draws, mostly), so treat the default as a
starting point, not a calibration for your own data.}
}
\value{
A tibble, one row per \code{worker_levels} entry: \code{n_workers},
\code{chunk_size}, \code{n_chunks}, \code{peak_mb}, \code{wall_seconds}, \code{core_seconds}
(the last four \code{NA} where infeasible).
}
\description{
Chunk size shrinks as \code{n_cores} rises (forking \code{n_workers} costs memory
roughly proportional to \code{n_workers}, out of the same \code{max_memory_mb}
budget -- see \code{\link[=.bilatr_chunk_overhead_multiplier]{.bilatr_chunk_overhead_multiplier()}}), and each chunk's
read is a full parse of the largest chain file (see
\link{.fast_read_post_warmup_draws}) -- so raising \code{n_workers} doesn't just
speed up the CPU-bound summary step, it also forces MORE chunks, i.e.
more full-file re-parses. Past some point this makes wall time worse,
not better, and core-seconds (\verb{n_workers * wall time}) rise
monotonically with \code{n_workers} even where wall time itself is still
falling -- nothing before this helper made either consequence visible
to a caller sizing a job.
}
\details{
For each candidate in \code{worker_levels}, resolves \code{chunk_size} (\code{\link[=.compute_chunk_size]{.compute_chunk_size()}})
and the resulting \code{n_chunks = ceiling(n_vars / chunk_size)}, estimated
peak memory (\code{\link[=.estimate_diagnostics_memory_mb]{.estimate_diagnostics_memory_mb()}}), estimated wall time
\code{n_chunks * read_seconds + n_vars * summarise_seconds_per_variable / n_workers}, and estimated core-seconds (\verb{n_workers * wall time}). A
\code{n_workers} level whose budget can't fit even one variable per chunk
(see \code{\link[=.compute_chunk_size]{.compute_chunk_size()}}'s own \code{stop()}) is reported as infeasible
(\code{NA} \code{chunk_size}/\code{n_chunks}/wall/core-seconds) rather than erroring
the whole table.
}
\keyword{internal}
