% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{assign_bilatr_class}
\alias{assign_bilatr_class}
\title{Assign the bilatr event class from a CAMEO event code}
\usage{
assign_bilatr_class(code, eventrootcode2 = assign_eventrootcode2(code))
}
\arguments{
\item{code}{Character or numeric vector of CAMEO event codes.}

\item{eventrootcode2}{Character vector of \code{EventRootCode2} values, as
returned by \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}}. Defaults to computing it from
\code{code}.}
}
\value{
Integer vector of \code{BilatrClass} values (0-10), \code{NA} for
unrecognized codes.
}
\description{
\code{BilatrClass} is an 11-level (0-10) collapse of the CAMEO taxonomy
used as the default action-class scheme for the bilatr model. It is
mostly a function of \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}} (see that function for
the root regrouping and \code{\link[=bilatr_class_name]{bilatr_class_name()}} for the level labels),
with a handful of per-code refinements where a specific event type
fits a different class than its regrouped root:
}
\details{
\itemize{
\item \code{016} "Deny responsibility" -> 8 (Disapprove).
\item \code{018} "Make empathetic comment" -> 4 (diplomatic cooperation).
\item \code{019} "Express accord" -> 1 (express intent to cooperate).
\item Appeals for political reform / to yield (\verb{024*}, \verb{025*}) ->
0 (neutral statement).
\item Appeals to others to meet / settle / mediate (\code{026}, \code{027},
\code{028}) -> 3 (consult: negotiate or mediate).
\item \code{041} "Discuss by telephone" -> 0 (neutral statement).
}
}
\keyword{internal}
