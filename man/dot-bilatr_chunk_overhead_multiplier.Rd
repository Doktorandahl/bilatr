% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.bilatr_chunk_overhead_multiplier}
\alias{.bilatr_chunk_overhead_multiplier}
\title{The \code{k} memory-model multiplier \code{\link[=.compute_chunk_size]{.compute_chunk_size()}}/
\code{\link[=.estimate_diagnostics_memory_mb]{.estimate_diagnostics_memory_mb()}} apply to a chunk's raw byte count}
\usage{
.bilatr_chunk_overhead_multiplier(n_chains, n_cores)
}
\arguments{
\item{n_chains}{From \link{.prepare_fast_csv_read}.}

\item{n_cores}{See \code{\link[=diagnose_convergence]{diagnose_convergence()}}'s \code{parallel}/\code{n_workers}.}
}
\value{
The multiplier \code{k}, in units of \code{raw_mb}.
}
\description{
The post-\link{.fast_read_post_warmup_draws} pipeline holds, per chunk of
\code{v} variables: the preallocated \code{array(n_draws, n_chains, v)} plus
\code{summarise_draws()}'s own working set, both sized for the full
\code{n_chains} array (\code{.BILATR_CHUNK_ARRAY_FACTOR}, ~3x \code{raw_mb});
\code{fread()}'s parse buffer and one chain's resulting matrix, each sized
for ONE chain (\code{raw_mb / n_chains}, i.e.
\code{.BILATR_CHUNK_PER_CHAIN_FACTOR / n_chains}); the array-native sign
flip's residual cost, budgeted unconditionally
(\code{.BILATR_CHUNK_FLIP_FACTOR}, see its own docs for why); and, only
when \code{n_cores > 1}, \code{posterior::summarise_draws()}'s forked-worker
cost -- a one-time step
(\code{.BILATR_CHUNK_CORES_STEP_FACTOR}) plus a per-additional-worker term
(\code{.BILATR_CHUNK_CORES_PER_CORE_FACTOR * (n_cores - 1)}), NOT a flat
penalty regardless of the exact core count (see that constant's docs
-- this replaced an assumption that was never measured and was
wrong).
}
\keyword{internal}
