% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{event_class_labels}
\alias{event_class_labels}
\title{Human-readable labels for a Stan data object's event classes}
\usage{
event_class_labels(stan_data, scheme = attr(stan_data, "grouping_var"))
}
\arguments{
\item{stan_data}{A Stan data list as returned by \code{\link[=assemble_stan_data]{assemble_stan_data()}}
(must carry its \code{"event_classes"} attribute).}

\item{scheme}{Name of a \link{cameo_lookup} column with a matching \verb{*Name}
column (see Details), or anything else to fall back to the raw event
classes. Defaults to \code{attr(stan_data, "grouping_var")}.}
}
\value{
A named character vector: names are \code{attr(stan_data, "event_classes")}, in order; values are the corresponding labels. An
event class missing from the lookup (possible for a real CAMEO
\code{scheme} if the classes came from a non-\code{cameo_lookup} source) keeps
its code as its own label, with a \code{warning()} naming it.
}
\description{
The data-driven replacement (0.10.1) for the retired per-scheme
\verb{*_name()} functions (\code{eventrootcode3_name()} and friends; see
NEWS.md): rather than a standalone namer per CAMEO regrouping that the
caller has to pick by hand, this reads labels straight out of
\link{cameo_lookup}'s \verb{*Name} column for \code{scheme}, keyed by event class
VALUE, never by position in \code{event_classes} -- so it is correct
regardless of where \code{reference_category} put a class in that order
(unlike the old \code{class_label_fn(action_index)} pattern this replaces;
see \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}'s \code{class_labels} argument).
}
\details{
\code{scheme} names a \link{cameo_lookup} column with a matching \verb{*Name} column:
currently \code{"QuadClass"} (\code{QuadClassName}), \code{"PentaClass"}
(\code{PentaClassName}), and \code{"ModifiedRootCode"} (\code{ModifiedRootCodeName}).
For any other \code{scheme} -- \code{NULL}, an unrecognized name (e.g.
\code{"PentaClass_modified"}, which has no \verb{*Name} column of its own), or a
non-CAMEO grouping variable from the caller's own data -- the event
classes' own codes are used as their labels, since there is no lookup
to consult; if \code{scheme} was non-\code{NULL} but unrecognized, a \code{message()}
names the schemes that do have labels.

\code{stan_data.rds} files saved before 0.9.0 have no \code{grouping_var}
attribute (added in 0.9.0), and the three current cluster runscripts'
specs name their \code{ModifiedRootCode} column \code{"ERC16NZ"} (its pre-0.8.0
name) rather than \code{"ModifiedRootCode"} -- in both cases, \code{scheme} must
be passed explicitly (\code{scheme = "ModifiedRootCode"}), since the
default \code{attr(stan_data, "grouping_var")} is either absent or not a
name \link{cameo_lookup} recognizes.
}
\examples{
\dontrun{
event_class_labels(stan_data)
event_class_labels(stan_data, scheme = "ModifiedRootCode") # pre-0.9.0 stan_data.rds
}
}
