% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category_merges.R
\name{merge_cost}
\alias{merge_cost}
\title{Price an explicit grouping of event categories}
\usage{
merge_cost(x, groups)
}
\arguments{
\item{x}{A \code{bilatr_category_merges} object, as returned by
\code{\link[=diagnose_category_merges]{diagnose_category_merges()}}.}

\item{groups}{A list of character vectors of \code{event_class} labels (or
integer \code{action_index} values), one per group to merge.}
}
\value{
A tibble with one row: \code{pct_info_lost} (mean), \code{pct_info_lost_lower},
\code{pct_info_lost_upper} (a posterior interval, \code{probs}' outer bounds as
used by \code{x}).
}
\description{
Sums \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}'s per-draw group-merge loss over an
explicit list of groupings (categories not mentioned stay singleton),
reusing \code{x}'s stored per-draw \code{alpha}/shares rather than re-reading the
fit -- so a scheme already in mind can be priced directly, without
walking the greedy ladder.
}
