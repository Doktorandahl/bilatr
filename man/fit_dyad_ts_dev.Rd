% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_dev.R
\name{fit_dyad_ts_dev}
\alias{fit_dyad_ts_dev}
\title{Development entry point: fit a single dyad with a selectable Stan model}
\usage{
fit_dyad_ts_dev(
  stan_data,
  chains = 4,
  parallel_chains = 4,
  threads_per_chain = 1,
  iter_warmup = 1000,
  iter_sampling = 1000,
  seed = NULL,
  opt_level = 2,
  output_dir = NULL,
  stan_model = .BILATR_DEFAULT_MODEL,
  ...
)
}
\arguments{
\item{stan_data}{A Stan data list from \code{\link[=assemble_stan_data]{assemble_stan_data()}} with
\code{D == 1}.}

\item{chains}{Number of MCMC chains.}

\item{parallel_chains}{Number of chains to run in parallel.}

\item{threads_per_chain}{Threads per chain for within-chain
parallelization. Not useful for \code{D == 1}; left as an argument mainly
for consistency with \code{\link[=fit_panel]{fit_panel()}}.}

\item{iter_warmup}{Number of warmup iterations per chain.}

\item{iter_sampling}{Number of post-warmup sampling iterations per
chain.}

\item{seed}{Random seed, or \code{NULL} for \code{cmdstanr}'s default.}

\item{opt_level}{Compiler optimization level; see
\code{\link[=compile_bilatr_model]{compile_bilatr_model()}}.}

\item{output_dir}{Directory to write CmdStan's raw output CSVs to, or
\code{NULL} for a temporary directory.}

\item{stan_model}{Name of a model registered in \code{.bilatr_stan_models}
(see \code{R/model_registry.R}). Currently registered: \code{"stable"} (the
default used by the exported \code{\link[=fit_dyad_ts]{fit_dyad_ts()}}) and \code{"phi_logn"}
(experimental: per-dyad-period \code{phi} as a function of centered
log event count). Unrecognized names error immediately, before any
compilation is attempted, listing the currently registered options.}

\item{...}{Additional arguments passed to \code{CmdStanModel$sample()}.}
}
\value{
A \code{CmdStanMCMC} fit object.
}
\description{
Identical to \code{\link[=fit_dyad_ts]{fit_dyad_ts()}} except for the \code{stan_model} argument.
Not exported; for use during model development only.
}
\keyword{internal}
