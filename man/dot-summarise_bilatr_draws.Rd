% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.summarise_bilatr_draws}
\alias{.summarise_bilatr_draws}
\title{Summarise draws with the fixed measure set the CSV-path functions use}
\usage{
.summarise_bilatr_draws(draws, n_cores = 1L)
}
\arguments{
\item{draws}{A \code{posterior::draws_array}/\code{draws_df} to summarise.}

\item{n_cores}{Passed through to \code{summarise_draws()}'s \code{.cores}:
\code{posterior::summarise_draws.draws()} splits \code{draws}' variables into
\code{n_cores} slices and forks one process per slice
(\code{parallel::mclapply()}, copy-on-write on Unix), so this parallelises
the CPU-bound Rhat/ESS computation, not the (sequential, disk-bound)
read that produced \code{draws}.}
}
\value{
A tibble, one row per variable in \code{draws}.
}
\description{
\code{mean}, \code{\link[posterior:quantile2]{posterior::quantile2()}} (default \code{q5}/\code{q95}), \code{median},
\code{rhat}, \code{ess_bulk}, \code{ess_tail} -- every column \code{.compute_tier1()}/
\code{.compute_tier2()}/\code{.compute_tier3()} (which each select their own
fixed subset regardless of what else is present) and
\code{diagnose_and_extract_bilatr()}'s \code{extract_from_summ()}/
\code{action_extract()} (which need \code{mean}/\code{q5}/\code{median}/\code{q95} by name)
actually use. \code{posterior::summarise_draws()}'s own defaults also
compute \code{sd} and \code{mad}, silently discarded by every caller today --
dropping them isn't free at the scale this package targets
(~1.9M Tier 3 variables per chain), so this is used everywhere a CSV
chunk gets summarised instead of relying on the defaults.
}
\keyword{internal}
