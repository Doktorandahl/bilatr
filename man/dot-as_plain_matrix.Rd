% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_check.R
\name{.as_plain_matrix}
\alias{.as_plain_matrix}
\title{Strip a \code{posterior::draws_matrix}'s class down to a plain base matrix}
\usage{
.as_plain_matrix(m)
}
\description{
\code{posterior::draws_matrix} objects define their own arithmetic (\code{Ops})
methods, which do not follow plain R's row-recycling rules -- the
\code{alpha_mat * theta_dt} (an \verb{n_draws x A} matrix times a length-
\code{n_draws} vector, relying on ordinary column-wise recycling) used
throughout \code{\link[=check_compositional_residuals]{check_compositional_residuals()}}'s per-dyad-period loop
needs a genuine base matrix, not a \code{draws_matrix}, or it errors with
"non-conformable arrays". Every draws matrix is stripped via this
immediately after subsetting to rows/columns of interest.
}
\keyword{internal}
