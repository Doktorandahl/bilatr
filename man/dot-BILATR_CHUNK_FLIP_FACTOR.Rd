% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\docType{data}
\name{.BILATR_CHUNK_FLIP_FACTOR}
\alias{.BILATR_CHUNK_FLIP_FACTOR}
\title{Memory-model constant: the post-Step-1 array-native sign flip's
residual cost}
\format{
An object of class \code{numeric} of length 1.
}
\usage{
.BILATR_CHUNK_FLIP_FACTOR
}
\description{
Applied unconditionally (not just when a flip actually happens):
whether \code{alpha[1]}'s posterior median is negative isn't known until
after the first chunk is read, so \code{\link[=.compute_chunk_size]{.compute_chunk_size()}} must budget
for the possibility on every call, not just the calls that end up
needing it. Since \code{\link[=.fast_read_post_warmup_draws]{.fast_read_post_warmup_draws()}}'s \code{flip_vars}
negates \code{flip_pos} columns of \code{m} in place before it's assigned into
\code{arr} (no extra copy there), and \code{\link[=.chunked_summarise_csv_with_orientation]{.chunked_summarise_csv_with_orientation()}}/
\code{\link[=.read_and_orient_draws_summary]{.read_and_orient_draws_summary()}}'s array-native
\code{draws[, , flip_cols] <- -draws[, , flip_cols, drop = FALSE]} negates
only the matched subset of an already-materialized array, the
remaining cost is R's own copy-on-modify for that replacement.
Measured (2026-09, \code{dev/bench_memory.R}): flip added ~91-142 MB over
otherwise-identical no-flip runs (~0.6-1.0x \code{raw_mb}) -- far below
the ~3.9x \code{raw_mb} the pre-Step-1 \code{as_draws_df()} round-trip cost
(see \verb{dev/refactor_verification_2026-09-10.md} section 1), but not
zero, so (per that document's own instruction) a term stays here
rather than assuming it away.
}
\keyword{internal}
