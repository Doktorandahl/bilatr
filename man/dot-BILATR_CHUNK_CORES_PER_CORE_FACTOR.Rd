% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\docType{data}
\name{.BILATR_CHUNK_CORES_PER_CORE_FACTOR}
\alias{.BILATR_CHUNK_CORES_PER_CORE_FACTOR}
\title{Memory-model constant: the additional cost of each forked worker
beyond the first, when \code{.cores > 1}}
\format{
An object of class \code{numeric} of length 1.
}
\usage{
.BILATR_CHUNK_CORES_PER_CORE_FACTOR
}
\description{
\code{posterior::summarise_draws(.cores = k)} forks \code{k} worker processes
via \code{parallel::mclapply()}, each computing rank-normalised Rhat/bulk
ESS/tail ESS (each of which builds several same-shaped intermediate
arrays) over its own slice of variables. This is NOT a flat cost
regardless of \code{n_cores}, as the pre-measurement model assumed:
measured (2026-09, \code{dev/bench_memory.R}, single chain, 60,000
Tier-3 columns, 300 draws, no flip, peak RSS summed across the whole
process tree via the \code{ps} package -- \verb{/usr/bin/time}'s own child-RSS
accounting does not see a further-forked grandchild's memory, and
was confirmed empirically to under-count this exact case) at
\code{n_cores} = 1/2/4/8: 1002 / 1822 / 2378 / 3571 MB. Fitting \verb{extra beyond n_cores = 1 baseline = STEP + PER_CORE * (n_cores - 1)} in
units of \code{raw_mb} (144 MB here) across the 2->4 and 4->8 intervals
gives \verb{PER_CORE ~= 1.9-2.1}, consistently; \verb{STEP ~= 3.0-3.3}. Both
rounded up here for margin. Summing RSS across a fork tree likely
over-counts pages the OS still shares copy-on-write between workers
(a SLURM cgroup would charge shared pages once, not once per
process), so this constant is a conservative upper bound, not a
precise physical-memory figure -- an acceptable direction of error
for a job-sizing floor, given the alternative is under-counting and
risking exactly the OOM this package's memory model exists to
prevent. \strong{Only measured up to \code{n_cores = 8}}: a caller sizing a job
with \code{n_workers} well beyond that (the audit's own per-chain example
used 24) is extrapolating this linear term 3x past its measured
range -- validate with \code{dev/bench_memory.R} at your actual \code{n_cores}
before trusting the estimate for a real SLURM allocation that large.
}
\keyword{internal}
