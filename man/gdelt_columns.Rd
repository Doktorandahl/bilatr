% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdelt.R
\name{gdelt_columns}
\alias{gdelt_columns}
\title{Named presets of GDELT columns}
\usage{
gdelt_columns(set = c("core", "actors", "geo", "all"))
}
\arguments{
\item{set}{One of \code{"core"} (the essentials: identifiers, both actors'
\code{Code}/\code{CountryCode}/\verb{Type1-3Code}, the event-code hierarchy,
\code{IsRootEvent}, and the numeric/impact fields), \code{"actors"} (\code{"core"}
plus every other \verb{Actor1*}/\verb{Actor2*} field: \verb{*Name},
\verb{*KnownGroupCode}, \verb{*EthnicCode}, \verb{*Religion1Code},
\verb{*Religion2Code}), \code{"geo"} (\code{"core"} plus every \verb{*Geo_*} field), or
\code{"all"} (every column, including \code{SOURCEURL}).}
}
\value{
A character vector of column names, in file order.
}
\description{
Name-vector presets over the full 58-column schema (\code{?bilatr_gdelt_layout}),
for use as \code{\link[=read_gdelt]{read_gdelt()}}'s \code{columns} argument.
}
\details{
GDELT's own \code{QuadClass} is deliberately left out of \code{"core"}:
\code{\link[=recode_cameo]{recode_cameo()}} attaches its own \code{QuadClass} derived from \code{EventCode},
and if both are present it keeps the existing column and warns --
correct, but easy to trip over if you expect \code{"core"} to include
GDELT's \code{QuadClass}. Request it explicitly (or via \code{"all"}) if you want
GDELT's own value instead of \code{recode_cameo()}'s.
}
\examples{
gdelt_columns("core")
}
