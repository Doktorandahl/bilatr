% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_check.R
\name{.stratified_dyad_sample}
\alias{.stratified_dyad_sample}
\title{Stratified sample of dyad indices by a volume statistic}
\usage{
.stratified_dyad_sample(n_d, n_target, n_strata, seed)
}
\arguments{
\item{n_d}{Numeric vector, one volume statistic per dyad (the pool
being sampled from, already restricted to eligible dyads).}

\item{n_target}{Target sample size (capped at \code{length(n_d)}).}

\item{n_strata}{Number of strata.}

\item{seed}{Seed for reproducibility.}
}
\value{
An integer vector of sampled positions into \code{n_d}, sorted
ascending, length \code{min(n_target, length(n_d))}.
}
\description{
Splits \code{1:length(n_d)} into \code{n_strata} roughly equal-sized strata by
\code{\link[dplyr:ntile]{dplyr::ntile()}} on \code{n_d} (tie-robust, unlike a quantile-breaks
approach, which can collapse strata when \code{n_d} has many repeated
values -- common for event counts), then samples \code{n_target} indices
total, spread as evenly as possible across strata, so the volume
gradient stays represented in the subsample (see
\code{\link[=check_compositional_residuals]{check_compositional_residuals()}}'s \code{n_dyads}/\code{n_strata} args).
}
\keyword{internal}
