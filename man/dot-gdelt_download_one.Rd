% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdelt.R
\name{.gdelt_download_one}
\alias{.gdelt_download_one}
\title{Download one GDELT file to \code{destfile}, verifying and staging via \code{.part}}
\usage{
.gdelt_download_one(url, destfile, md5_lookup, verify)
}
\value{
A list with \code{status} (\code{"downloaded"}, \code{"missing"}, \code{"failed"},
or \code{"corrupt"}) and \code{path} (\code{destfile} if downloaded, else \code{NA}).
}
\description{
Exactly one request: \code{utils::download.file(mode = "wb")} to
\verb{<destfile>.part}, renamed to \code{destfile} only on success, so a failed
or interrupted download never leaves a file at the final path. Never
\code{try()}-and-return-the-path (audit G2): a failed/missing/corrupt result
becomes a status, with no warning raised here (the caller raises one
summary warning listing every failure).
}
\keyword{internal}
