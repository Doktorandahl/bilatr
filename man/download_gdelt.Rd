% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdelt.R
\name{download_gdelt}
\alias{download_gdelt}
\title{Download raw GDELT export zips}
\usage{
download_gdelt(
  start,
  end = start,
  dest_dir = NULL,
  overwrite = FALSE,
  timeout = 3600,
  verify = TRUE,
  ...
)
}
\arguments{
\item{start, end}{See \code{\link[=gdelt_files]{gdelt_files()}}.}

\item{dest_dir}{\code{NULL} (default, temp mode) or a directory path (cache
mode).}

\item{overwrite}{In cache mode, re-download files already present.
Ignored in temp mode.}

\item{timeout}{Seconds before a single download times out, raised via
a locally-scoped \code{options(timeout = timeout)} (restored on exit) --
R's default 60s truncates large backfiles.}

\item{verify}{Verify each downloaded (and, in cache mode, each
already-cached) file's MD5 against GDELT's \code{md5sums} index. If the
index itself can't be fetched, warns once and proceeds unverified.}

\item{...}{In temp mode, passed through to \code{read_gdelt()}. Ignored in
cache mode.}
}
\value{
See "Details" above.
}
\description{
Lists the files covering \verb{[start, end]} with \code{\link[=gdelt_files]{gdelt_files()}}, then
downloads each (this fixes audit G2/G3: one request per file, under a
locally-raised, generous \code{timeout}, never a swallowed failure).
}
\details{
\strong{\code{dest_dir = NULL}} (default): each file is downloaded to a
temporary path, read with \verb{read_gdelt(<tmp>, ...)} (\code{...} passed
through), and deleted -- so this mode needs no folder and leaves no
zips behind. Returns the row-bound, filtered events; the per-file
status table (\code{gdelt_files()}'s columns plus \code{status}) is attached as
\code{attr(result, "files")}.

\strong{\code{dest_dir = "some/dir"}}: creates the folder if needed and keeps the
zips as a cache. A file already present is skipped (verified against
\code{md5sums} too, if \code{verify = TRUE}) unless \code{overwrite = TRUE}. Data is
\strong{not} read in this mode -- call \code{read_gdelt(status$path[status$status \%in\% c("downloaded", "cached")], ...)} yourself. Returns (invisibly)
\code{gdelt_files()}'s columns plus \code{path} (\code{NA} for anything not on disk)
and \code{status} (one of \code{"downloaded"}, \code{"cached"}, \code{"missing"} -- HTTP
404, no such file on the server -- \code{"failed"}, or \code{"corrupt"}).

A failed/missing/corrupt file never aborts the whole call: it becomes
a status, and one summary \code{warning()} at the end lists every such
file. Downloads run sequentially (GDELT is a shared public server), with
a one-line progress \code{message()} per file.
}
\examples{
\dontrun{
# temp mode: a quick look, nothing kept on disk
events <- download_gdelt("2020-01-01")

# cache mode: for real work -- keep the zips, read them yourself
status <- download_gdelt("2020-01-01", "2020-01-31", dest_dir = "data/gdelt_raw")
events <- read_gdelt(status$path[status$status \%in\% c("downloaded", "cached")])
}
}
