% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_check.R
\name{.drop_nonfinite_draws}
\alias{.drop_nonfinite_draws}
\title{Drop posterior draws with non-finite compositional-residual accumulators}
\usage{
.drop_nonfinite_draws(
  T_obs_acc,
  T_rep_acc,
  cat_contrib_obs,
  cat_contrib_rep,
  sum_theta_bar_sq,
  sum_theta_sq,
  min_draws = 20L
)
}
\arguments{
\item{T_obs_acc, T_rep_acc}{\verb{n_draws x n_eps} pooled-statistic
accumulators.}

\item{cat_contrib_obs, cat_contrib_rep}{\verb{A x n_draws} per-category
accumulators.}

\item{sum_theta_bar_sq, sum_theta_sq}{Length-\code{n_draws} accumulators.}

\item{min_draws}{Minimum number of surviving draws before this
\code{stop()}s instead of warning. Default \code{20}.}
}
\value{
A list of the same objects, each subset to the surviving
draws, plus \code{n_draws_dropped}.
}
\description{
A defensive backstop for \code{\link[=check_compositional_residuals]{check_compositional_residuals()}}: both known
sources of non-finite \code{clr()} residuals are fixed at the source
(\code{.rdirichlet_rows()}'s Dirichlet-gamma underflow, \code{.clr()}'s
zero-component log; see \code{NEWS.md} 0.6.1), but a multi-hour production
run should not die on its last step if some future, unanticipated
source slips a non-finite value through. Rather than \code{na.rm = TRUE}
(which would hide the problem silently -- how the original crash
reached production undetected), whole draws with any non-finite entry
are dropped from every pooled statistic, with a single warning naming
how many, and a hard \code{stop()} if too few draws remain to trust the
result.
}
\keyword{internal}
