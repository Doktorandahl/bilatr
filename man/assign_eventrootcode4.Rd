% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{assign_eventrootcode4}
\alias{assign_eventrootcode4}
\title{Regroup a CAMEO event code into the bilatr "EventRootCode4" scheme}
\usage{
assign_eventrootcode4(code)
}
\arguments{
\item{code}{Character or numeric vector of CAMEO event codes (e.g.
\code{"0211"}, \code{"19"}).}
}
\value{
Integer vector of \code{EventRootCode4} values (1-23).
}
\description{
\code{EventRootCode4} is a further refinement of \code{\link[=assign_eventrootcode3]{assign_eventrootcode3()}},
splitting three of its remaining merged/aggregated groupings back out
into their individual root/sub-codes, while keeping its relocations of
\code{016}/\code{018}/\code{019}:
}
\details{
\itemize{
\item \code{EventRootCode3}'s "Consult: meet, discuss, or visit" class
(\code{041}-\code{044}) is split so that \code{041} ("Discuss by telephone")
becomes its own class, separate from \code{042}-\code{044}.
\item \code{EventRootCode3}'s "Threaten or exhibit force posture" class
(roots 13 + 15) is split back into Threaten (root 13) and Exhibit
force posture (root 15); Protest (root 14) was already its own
class in \code{\link[=assign_eventrootcode3]{assign_eventrootcode3()}} and is unaffected.
\item \code{EventRootCode3}'s "Assault, fight, or mass violence" class
(roots 18 + 19 + 20) is split back into Assault (root 18), Fight
(root 19), and Use unconventional mass violence (root 20).
\item \code{016} ("Deny responsibility") still moves out of root 01 into
the Reject class (with root 12); \code{018}/\code{019} ("Make empathetic
comment"/"Express accord") still move out of root 01 into the
diplomatic-cooperation class (with root 05) -- same relocations
as \code{\link[=assign_eventrootcode3]{assign_eventrootcode3()}}.
\item Every other root maps as in \code{\link[=assign_eventrootcode3]{assign_eventrootcode3()}}.
}

Classes are numbered 1-23 in ascending root-code order; see
\code{\link[=eventrootcode4_name]{eventrootcode4_name()}} for labels and \code{\link[=eventrootcode4_rootcodes]{eventrootcode4_rootcodes()}}
for the underlying CAMEO root/event codes each class draws from.
}
\keyword{internal}
