% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{eventrootcode2_name}
\alias{eventrootcode2_name}
\title{Human-readable label for an \code{EventRootCode2} value}
\usage{
eventrootcode2_name(class)
}
\arguments{
\item{class}{Character vector of \code{EventRootCode2} values, as returned
by \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}}.}
}
\value{
Character vector of the corresponding class names, \code{NA} for
values \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}} does not produce.
}
\description{
Preliminary labels, one per \code{\link[=assign_eventrootcode2]{assign_eventrootcode2()}} output value.
Where an \code{EventRootCode2} value corresponds exactly to a \code{BilatrClass}
level (the two root-04 splits \code{"044"}/\code{"046"}), the label text matches
\code{\link[=bilatr_class_name]{bilatr_class_name()}}'s for that level, since they denote the same
category; every other label is new here, since \code{EventRootCode2} is
finer-grained than \code{BilatrClass} everywhere else (e.g. \code{"06"}/\code{"07"}
get distinct labels despite both folding into \code{BilatrClass}'s single
"material cooperation or provide aid").
}
\keyword{internal}
