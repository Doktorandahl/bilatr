% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_alpha}
\alias{extract_alpha}
\title{Extract discrimination parameters (alpha) with action-class labels}
\usage{
extract_alpha(
  fit,
  event_classes = NULL,
  probs = c(0.05, 0.5, 0.95),
  stan_model = .BILATR_DEFAULT_MODEL,
  scratch_dir = NULL
)
}
\arguments{
\item{fit}{A \code{CmdStanMCMC} fit object from \code{\link[=fit_dyad_ts]{fit_dyad_ts()}} or
\code{\link[=fit_panel]{fit_panel()}}, or a character vector of raw CmdStan CSV file paths
(see Details).}

\item{event_classes}{Optional character vector of event-class labels,
in the same order used to build \code{stan_data} (i.e. the
\code{"event_classes"} attribute attached by \code{\link[=assemble_stan_data]{assemble_stan_data()}}). If
supplied, an \code{event_class} column is added alongside the raw action
index.}

\item{probs}{Posterior quantiles to report alongside the mean. Only
the default is supported when \code{fit} is CSV file paths (see Details).}

\item{stan_model}{Name registered in \code{.bilatr_stan_models} identifying
which model produced \code{fit}; see \code{\link[=bilatr_orient]{bilatr_orient()}}. Defaults to
\code{.BILATR_DEFAULT_MODEL} (\code{"stable"}), matching what \code{\link[=fit_dyad_ts]{fit_dyad_ts()}}/
\code{\link[=fit_panel]{fit_panel()}} always fit.}

\item{scratch_dir}{See \code{\link[=diagnose_convergence]{diagnose_convergence()}}. Only used when \code{fit}
is CSV file paths.}
}
\value{
A tibble with one row per action type: \code{action_index}
(and \code{event_class} if \code{event_classes} is supplied), the posterior
\code{mean} of alpha, and one column per requested quantile.
}
\description{
Pulls posterior summaries of the action-type discrimination parameters
\code{alpha} out of a fitted model. \code{alpha} sums to exactly 0 and has RMS 1
(a \code{sum_to_zero_vector}, not a single fixed-to-1 element) for
\code{stable}/\code{ou}; \code{alpha[1]}, the reference/neutral action class supplied
via \code{reference_category}, is only softly anchored positive (see
\code{\link[=assemble_stan_data]{assemble_stan_data()}}'s \code{anchor_scale}). See the package's
identification notes in \code{vignette("dyad_time_series")}.
}
\details{
Draws are passed through \code{\link[=bilatr_orient]{bilatr_orient()}} before summarizing, so
\code{alpha}'s sign always reflects the canonical orientation regardless of
which reflection-symmetry basin the sampler actually landed in --
\code{stable}/\code{ou}'s soft anchor above doesn't guarantee that on its own.

\code{fit} also accepts a character vector of raw CmdStan CSV file paths
(one per chain), for the case where there is no in-memory fit at all
-- e.g. a post-hoc, cross-chain roll-up script run after several
independently-submitted single-chain SLURM jobs have all completed,
with only their saved CSVs on disk. Unlike \code{\link[=extract_theta]{extract_theta()}}'s
CSV-path mode, \code{alpha} is small (Tier 1: a handful of values
regardless of dyad-set size), so there is no chunking/\code{parallel}
machinery here -- just one plain read (via \link{.get_draws}) across all
of \code{fit}, then the same extraction logic either way.
}
\examples{
\dontrun{
alpha <- extract_alpha(fit, event_classes = attr(stan_data, "event_classes"))

# cross-chain roll-up from saved CSVs, no in-memory fit
csv_files <- list.files("model_output/some_spec", pattern = "\\\\.csv$", full.names = TRUE)
alpha <- extract_alpha(csv_files, event_classes = event_classes, stan_model = "ou")
}
}
