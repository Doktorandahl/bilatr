% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_data.R
\name{assemble_stan_data}
\alias{assemble_stan_data}
\title{Assemble Stan-ready data for the bilatr dyadic IRT model}
\usage{
assemble_stan_data(
  data,
  years,
  resolution = c("monthly", "yearly"),
  grouping_var,
  directed = TRUE,
  reference_category = NULL,
  min_n_events = 1,
  weighted = FALSE,
  chunk_size = 100,
  rho_prior_a = 8,
  rho_prior_b = 2,
  compute_log_lik = 0,
  prior_only = 0,
  compute_theta_filtered = 0,
  filter_dyads = NULL,
  anchor_scale = 0.1,
  actor1 = "Actor1CountryCode",
  actor2 = "Actor2CountryCode",
  date = "SQLDATE"
)
}
\arguments{
\item{data}{A data frame of event-level records; see
\code{?bilatr_event_data}. Two helpers produce data in this format:
\code{\link[=extract_all_relevant_gdelt]{extract_all_relevant_gdelt()}} and \code{\link[=recode_cameo]{recode_cameo()}}.}

\item{years}{Integer vector of years to cover. Rows outside \code{years} are
dropped from \code{data} before anything else -- including before the
class order is built (0.9.0; previously an action class seen only
outside \code{years} still got an all-zero column of \code{Y}, identified only
by its prior). A \code{message()} reports how many rows were dropped, and
names any class present in \code{data} that has zero in-window events (and
so is absent from \code{event_classes}/\code{A}/\code{Y}).}

\item{resolution}{Either \code{"monthly"} or \code{"yearly"}.}

\item{grouping_var}{Name of the event-class column to aggregate on
(e.g. \code{"QuadClass"}, \code{"PentaClass"}).}

\item{directed}{If \code{TRUE} (default), dyads are directed; if \code{FALSE},
actor order is ignored.}

\item{reference_category}{Value of \code{grouping_var} to anchor as the
model's scale/sign reference: \code{stable}/\code{ou} fold \code{alpha[1]}'s sign
into the reported \code{alpha}/\code{theta} (see each \code{.stan} file's header,
"IDENTIFICATION: ORIENTATION FOLD"), and RMS-normalize the whole
\code{alpha} vector to 1, so positive \code{alpha[1]} means better relations at
this reference/neutral class. Should typically be a
low-conflict/cooperative class. Every other action class's
discrimination (\code{alpha[2:A]}) is freely estimated. \code{NULL} (default)
anchors on the first class in C-locale numeric sort instead; if
supplied but absent from the (in-window) data, errors (0.9.0; see
\code{\link[=validate_reference_class]{validate_reference_class()}}).}

\item{min_n_events}{Minimum total events for a dyad to be retained.}

\item{weighted}{Defunct. The \code{dyad_weight}/\code{period_weight}/
\code{action_weight} likelihood-weighting scheme was removed in 0.4.6 (see
NEWS.md) -- never used in production, and \code{action_weight} in
particular made the Dirichlet-multinomial concentration depend on
\code{theta}, complicating the hand-differentiated forward filter built on
top of this likelihood. Kept only as a formal so old call sites that
actually requested weighting fail loudly instead of silently fitting
an unweighted model: must be \code{FALSE} or \code{"none"} (both accepted as
the only meaningful values now, matching this project's own SLURM
runscripts, which map their own \code{weighted = "none"} CLI argument to
\code{FALSE} before calling this function); any other value errors.}

\item{chunk_size}{\code{reduce_sum} grainsize used to chunk the likelihood
across dyads. 16 cores with \code{chunk_size = 600} was the
Pareto-optimal setting found in this project's own benchmarking for
panel-sized data; smaller panels or single-dyad fits should use a
smaller value (down to 1) since chunking overhead outweighs the
benefit below that scale. See \code{\link[=fit_panel]{fit_panel()}} for the corresponding
\code{threads_per_chain} argument.}

\item{rho_prior_a, rho_prior_b}{Shape parameters of the
\code{beta(rho_prior_a, rho_prior_b)} prior on the OU/AR(1) persistence
parameter \code{rho}, consumed only by the experimental \code{ou} Stan model
variant (see \code{R/model_registry.R}); ignored by \code{stable}, which keeps
a random-walk \code{theta} with no persistence parameter. Default \verb{8, 2}
(weighted toward strong persistence).}

\item{compute_log_lik}{\code{0} (default) or \code{1}. Data flag consumed by
both registered Stan model variants, gating a per-dyad-period
\code{log_lik} in \verb{generated quantities}; left off by default since it is
\verb{D x T x} draws and file size already scales with dyad count.}

\item{prior_only}{\code{0} (default) or \code{1}. Data flag consumed by both
registered Stan model variants, gating the \code{reduce_sum} likelihood
call in \code{model} exactly as \code{compute_log_lik} gates its generated
quantity -- \code{1} fits the prior alone, useful for prior-predictive
checks and for validating \code{\link[=alpha_prior_moments]{alpha_prior_moments()}} against an actual
fit.}

\item{compute_theta_filtered}{\code{0} (default) or \code{1}. Data flag consumed
by both registered Stan model variants, gating
\code{theta_filtered}/\code{theta_filtered_sd} in \verb{generated quantities}: the
forward-filtered state (conditional on each draw's hyperparameters,
a West-Harrison/Fisher-scoring linear-Bayes update; see each \code{.stan}
file's \verb{generated quantities} block) as opposed to the smoothed
\code{theta} every fit already returns. Left off by default: computing it
for every dyad is \code{D * T} iterations with \code{A} \code{digamma()} calls each,
single-threaded (no \code{reduce_sum} in \verb{generated quantities}), on the
order of a minute or two per chain for a full production-sized dyad
set, and adds two more \verb{D x T}-scaled Tier 3 blocks to the output.}

\item{filter_dyads}{Optional character vector of dyad names (matched
against the \code{dyad_ids} attribute's \code{dyad} column -- the directed
\code{"AAA_BBB"} key, same as everywhere else in this package) naming
which dyads to compute \code{theta_filtered} for. Ignored if
\code{compute_theta_filtered} is \code{0}. \code{NULL} (default) means all dyads,
once \code{compute_theta_filtered = 1} is set -- pass a small subset (e.g.
fifty dyads) to compute the filter cheaply for just those. Errors,
listing the offending names, if any requested dyad was dropped by
\code{min_n_events} or never existed.}

\item{anchor_scale}{Scale of the soft sign anchor
\verb{target += log_inv_logit(alpha[1] * inv(anchor_scale))}. Since 0.4.2
(see NEWS.md), this is consumed only by the LEGACY
\code{stable_soft_anchor}/\code{ou_soft_anchor} Stan programs (see
\code{R/model_registry.R}), which normalize \code{alpha} via a free
\code{sum_to_zero_vector} with no fixed element -- leaving an exact
reflection symmetry (\code{alpha}, \code{theta} -> \code{-alpha}, \code{-theta} is
likelihood-invariant) the anchor only softly penalizes (\code{alpha[1] < 0}), not reliably breaks across independently-initialized chains;
see \code{bilatr_orient()} for the post-hoc fix those two programs still
need. The current \code{stable}/\code{ou} programs fold \code{alpha[1]}'s sign into
the reported \code{alpha}/\code{theta} instead (see each \code{.stan} file's
header, "IDENTIFICATION: ORIENTATION FOLD") and don't declare
\code{anchor_scale} at all; it is still supplied unconditionally
here (CmdStan ignores data a program doesn't declare), so this
function doesn't need to branch on \code{stan_model}. \code{alpha[1]} is
already the reference/neutral action class supplied via
\code{reference_category} -- no separate index is needed. Default \code{0.1}.}

\item{actor1, actor2}{Names of the sender/target actor columns; see
\code{?bilatr_event_data}.}

\item{date}{Name of the event-date column; see \code{?bilatr_event_data}.}
}
\value{
A named list suitable as the \code{data} argument to
\code{cmdstanr::CmdStanModel$sample()} for the bilatr Stan model: \code{D},
\code{T}, \code{A}, \code{C}, \code{is_obs}, \code{Y}, \code{rho_prior_a}, \code{rho_prior_b},
\code{compute_log_lik}, \code{prior_only}, \code{compute_theta_filtered},
\code{n_filter_dyads}, \code{filter_dyads}, \code{anchor_scale}, and (0.7.0,
unconditionally -- see the fields' own inline comments in this
function's body) \code{n_countries}, \code{ctry_a}, \code{ctry_b}, \code{w_send} (only
consumed by the experimental \code{stable_gamma} variant; see
\code{R/model_registry.R}). \code{n_countries} counts only the countries
actually present in the RETAINED dyads (after \code{min_n_events}
filtering) -- a country appearing only in a dropped dyad consumes no
index. \code{ctry_a}/\code{ctry_b} are 1-indexed into that country set, in
\code{dyad_id} order (\code{ctry_a} is the sender for directed dyads, side A
for undirected); \code{w_send} is per-dyad (not per dyad-period -- see
\code{\link[=grouped_events_to_dyad_period]{grouped_events_to_dyad_period()}}'s \code{w_send} attribute), exactly \code{1}
for every directed dyad. A \code{message()} at assembly time reports
\code{n_countries} and the dyads-per-country distribution (min/median/
max), so a country identified from only one or two dyads (whose
\code{gamma_c} is then close to confounded with those dyads' own \code{theta})
is visible before fitting, not after. Also carries a \code{dyad_ids}
attribute (the output of \code{\link[=make_dyad_ids]{make_dyad_ids()}}) for reattaching
identifiers to posterior draws (see \code{\link[=extract_theta]{extract_theta()}}), an
\code{event_classes} attribute, a \code{grouping_var} attribute (0.9.0; the
\code{grouping_var} argument itself, for downstream class-label
derivation), and (0.7.0) a \code{country_codes} attribute (character, in
\code{ctry_a}/\code{ctry_b} index order) for labelling \code{gamma} (see
\code{\link[=extract_gamma]{extract_gamma()}}).
}
\description{
Aggregates event data to dyad-period action-class counts and packages
it as the data list expected by the package's Stan model (the
registered \code{stable} program; see \code{R/model_registry.R}).
}
\details{
Validates \code{data} against \code{?bilatr_event_data} first, drops rows outside
\code{years} before the set of observed event classes is determined (see
\verb{@param years}), then delegates the aggregation itself to
\code{\link[=grouped_events_to_dyad_period]{grouped_events_to_dyad_period()}}.
}
\examples{
\dontrun{
events <- extract_all_relevant_gdelt("data/gdelt_raw/20200101.zip")
events <- recode_cameo(events)
agg <- grouped_events_to_dyad_period(
  events,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0
)
stan_data <- assemble_stan_data(
  events,
  years = 2015:2020,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0
)
}
}
