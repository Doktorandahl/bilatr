% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_data.R
\name{assemble_stan_data}
\alias{assemble_stan_data}
\title{Assemble Stan-ready data for the bilatr dyadic IRT model}
\usage{
assemble_stan_data(
  data,
  years,
  resolution = c("monthly", "yearly"),
  grouping_var,
  directed = TRUE,
  reference_category = NULL,
  min_n_events = 1,
  weighted = FALSE,
  dyad_weight = NULL,
  period_weight = NULL,
  action_weight = NULL,
  chunk_size = 100,
  rho_prior_a = 8,
  rho_prior_b = 2,
  compute_log_lik = 0
)
}
\arguments{
\item{data}{A data frame of event-level records, as produced by
\code{\link[=extract_all_relevant_gdelt]{extract_all_relevant_gdelt()}} or \code{\link[=ingest_icews]{ingest_icews()}} and recoded via
\code{\link[=recode_cameo]{recode_cameo()}}.}

\item{years}{Integer vector of years to cover.}

\item{resolution}{Either \code{"monthly"} or \code{"yearly"}.}

\item{grouping_var}{Name of the event-class column to aggregate on
(e.g. \code{"QuadClass"}, \code{"PentaClass"}).}

\item{directed}{If \code{TRUE} (default), dyads are directed; if \code{FALSE},
actor order is ignored.}

\item{reference_category}{Value of \code{grouping_var} to anchor as the
model's scale/sign reference (\code{alpha[1] = 1}). Should typically be
a low-conflict/cooperative class. Every other action class's
discrimination (\code{alpha[2:A]}) is freely estimated.}

\item{min_n_events}{Minimum total events for a dyad to be retained.}

\item{weighted}{\code{FALSE} (default), or a single string naming which
default reweighting scheme(s) to compute via
\code{\link[=compute_default_weights]{compute_default_weights()}} for any of \code{dyad_weight}/\code{period_weight}/
\code{action_weight} left as \code{NULL}: \code{"all"} for all three, or a
\code{"-"}-separated combination of \code{"dyad"}, \code{"period"}, \code{"action"} for
just those (e.g. \code{"dyad"}, \code{"dyad-period"}, \code{"period-action"}).
Components not named here, and not requested, still default to 1s.
\code{weighted = TRUE} is no longer accepted; use \code{weighted = "all"}.}

\item{dyad_weight}{Optional numeric vector of length D (number of
retained dyads) rescaling each dyad's contribution to the
likelihood. Defaults to 1s (or computed, if \code{"dyad"} is requested via
\code{weighted}).}

\item{period_weight}{Optional numeric vector of length T (number of
time periods) rescaling each period's contribution. Defaults to 1s
(or computed, if \code{"period"} is requested via \code{weighted}).}

\item{action_weight}{Optional numeric vector of length A (number of
action classes) rescaling the concentration per action type.
Defaults to 1s (or computed, if \code{"action"} is requested via
\code{weighted}).}

\item{chunk_size}{\code{reduce_sum} grainsize used to chunk the likelihood
across dyads. 16 cores with \code{chunk_size = 600} was the
Pareto-optimal setting found in this project's own benchmarking for
panel-sized data; smaller panels or single-dyad fits should use a
smaller value (down to 1) since chunking overhead outweighs the
benefit below that scale. See \code{\link[=fit_panel]{fit_panel()}} for the corresponding
\code{threads_per_chain} argument.}

\item{rho_prior_a, rho_prior_b}{Shape parameters of the
\code{beta(rho_prior_a, rho_prior_b)} prior on the OU/AR(1) persistence
parameter \code{rho}, consumed only by the experimental \code{ou}/\code{alphanorm_ou}
Stan model variants (see \code{R/model_registry.R}); ignored by \code{stable}
and \code{alphanorm}. Default \verb{8, 2} (weighted toward strong persistence).}

\item{compute_log_lik}{\code{0} (default) or \code{1}. Data flag consumed only by
the three experimental variants, gating a per-dyad-period \code{log_lik}
in \verb{generated quantities}; left off by default since it is \verb{D x T x}
draws and file size already scales with dyad count. Ignored by
\code{stable}.}
}
\value{
A named list suitable as the \code{data} argument to
\code{cmdstanr::CmdStanModel$sample()} for the bilatr Stan model: \code{D},
\code{T}, \code{A}, \code{C}, \code{is_obs}, \code{Y}, \code{dyad_weight}, \code{period_weight},
\code{action_weight}, \code{rho_prior_a}, \code{rho_prior_b}, \code{compute_log_lik}.
Also carries a \code{dyad_ids} attribute (the output of
\code{\link[=make_dyad_ids]{make_dyad_ids()}}) for reattaching identifiers to posterior draws;
see \code{\link[=extract_theta]{extract_theta()}}.
}
\description{
Aggregates CAMEO-coded event data to dyad-period action-class counts
and packages it as the data list expected by the package's Stan model
(\code{inst/stan/bilatr_dirmult_irt.stan}). This single function covers all
three previously-separate model variants (base, event-weighted,
dyad+period+event-weighted): \code{dyad_weight}, \code{period_weight}, and
\code{action_weight} default to vectors of 1s (recovering the base,
unweighted model), can be supplied explicitly, or computed
automatically per-component via \code{weighted}.
}
\examples{
\dontrun{
events <- extract_all_relevant_gdelt("data/gdelt_raw/20200101.zip")
events <- recode_cameo(events)
agg <- grouped_events_to_dyad_period(
  events,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0
)
stan_data <- assemble_stan_data(
  events,
  years = 2015:2020,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0
)

# compute default dyad- and period-level reweighting, but leave
# action_weight at 1s:
stan_data_dp <- assemble_stan_data(
  events,
  years = 2015:2020,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0,
  weighted = "dyad-period"
)

# compute all three default reweighting schemes:
stan_data_all <- assemble_stan_data(
  events,
  years = 2015:2020,
  resolution = "yearly",
  grouping_var = "PentaClass",
  reference_category = 0,
  weighted = "all"
)
}
}
