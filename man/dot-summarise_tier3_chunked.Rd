% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.summarise_tier3_chunked}
\alias{.summarise_tier3_chunked}
\title{Summarise Tier 3 variables in memory-bounded chunks}
\usage{
.summarise_tier3_chunked(csv_files, variables, chunk_size, parallel, n_workers)
}
\arguments{
\item{csv_files}{Character vector of CmdStan CSV file paths.}

\item{variables}{Character vector of Tier 3 variable names to
summarise.}

\item{chunk_size}{Variables per chunk.}

\item{parallel, n_workers}{See \code{\link[=diagnose_convergence]{diagnose_convergence()}}.}
}
\value{
A tibble, the row-bound \code{\link[posterior:draws_summary]{posterior::summarise_draws()}} output
across all chunks.
}
\description{
Reads and summarises \code{variables} in groups of \code{chunk_size}, discarding
each chunk's draws before moving to the next -- this is what keeps
peak memory bounded regardless of how many Tier 3 variables there are
in total. \code{parallel = TRUE} processes chunks concurrently via
\code{furrr::future_map_dfr()}, trading the sequential path's memory bound
(now multiplied by \code{n_workers}, since that many chunks are in memory
at once) for wall-clock speed. The active \code{future::plan()} is saved
and restored on exit, so this never permanently changes the caller's
parallel backend.
}
\keyword{internal}
