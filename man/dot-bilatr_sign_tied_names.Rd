% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orient.R
\name{.bilatr_sign_tied_names}
\alias{.bilatr_sign_tied_names}
\title{Raw and derived variable names the alpha/theta reflection symmetry
ties to alpha's sign, per registered model}
\usage{
.bilatr_sign_tied_names(stan_model)
}
\arguments{
\item{stan_model}{Name registered in \code{.bilatr_stan_models}, or a
recognized pre-0.4.0 alias (see \code{\link[=.canonical_stan_model]{.canonical_stan_model()}}, called
here first).}
}
\value{
A list with elements \code{raw} and \code{derived}, each a character
vector of variable base names (matched against \code{posterior} draws
column names via \code{\link[=.bilatr_match_draws_columns]{.bilatr_match_draws_columns()}}); both
\code{character(0)} for any model with no such symmetry.
}
\description{
\code{raw} is the set of actually-sampled parameters whose sign is
ambiguous under the symmetry (present, and sign-ambiguous, in every
registered model regardless of what -- if anything -- fixes the
REPORTED quantities downstream). \code{derived} is the set of transformed
parameters built from them that inherit the same ambiguity UNLESS
something corrects it: for the legacy \verb{_soft_anchor} models, nothing
does, so \code{derived} is exactly what still needs \code{bilatr_orient()}; for
\code{stable}/\code{ou}, the orientation fold corrects \code{derived} automatically
(see each \code{.stan} file's header, "IDENTIFICATION: ORIENTATION FOLD"),
so it is never combined with \code{raw} for them (see
\code{\link[=.bilatr_flip_variables]{.bilatr_flip_variables()}}).
}
\keyword{internal}
