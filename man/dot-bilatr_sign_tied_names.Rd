% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sign_ambiguity.R
\name{.bilatr_sign_tied_names}
\alias{.bilatr_sign_tied_names}
\title{Raw, sampled parameter names the alpha/theta reflection symmetry ties
to alpha's sign, per registered model}
\usage{
.bilatr_sign_tied_names(stan_model)
}
\arguments{
\item{stan_model}{Name registered in \code{.bilatr_stan_models}; see
\code{\link[=.canonical_stan_model]{.canonical_stan_model()}}.}
}
\value{
A list with element \code{raw}, a character vector of variable base
names (matched against \code{posterior} draws column names); \code{character(0)}
for any model with no such symmetry.
}
\description{
Present, and sign-ambiguous, in every registered model regardless of
whether the current orientation fold fixes the REPORTED quantities
built from them -- it does, for every model registered since 0.10.0,
but the raw parameters themselves stay ambiguous.
}
\keyword{internal}
