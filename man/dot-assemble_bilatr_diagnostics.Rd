% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.assemble_bilatr_diagnostics}
\alias{.assemble_bilatr_diagnostics}
\title{Assemble a \code{bilatr_diagnostics} object from a tier-classified summary}
\usage{
.assemble_bilatr_diagnostics(
  summ,
  n_dt_tbl,
  tiers,
  rhat_threshold,
  ess_threshold
)
}
\arguments{
\item{summ}{A tibble as produced by \code{\link[posterior:draws_summary]{posterior::summarise_draws()}},
left-joined with \link{.classify_bilatr_tier}'s \code{tier}/\code{dyad_id}/
\code{time_index} columns.}

\item{n_dt_tbl}{Output of \link{.normalize_n_dt}, or \code{NULL} if \code{tiers}
excludes both 2 and 3.}

\item{tiers}{Validated (via \link{.validate_tiers}) tiers to compute.}

\item{rhat_threshold, ess_threshold}{See \code{\link[=diagnose_convergence]{diagnose_convergence()}}.}
}
\value{
A list of class \code{bilatr_diagnostics}; see
\code{\link[=diagnose_convergence]{diagnose_convergence()}}'s \verb{@return} for the element-by-element
description.
}
\description{
Shared by both branches of \code{\link[=diagnose_convergence]{diagnose_convergence()}} (in-memory fit and
raw CmdStan CSV files): everything past "we now have a
\code{summarise_draws()}-shaped tibble with \code{tier}/\code{dyad_id}/\code{time_index}
columns joined on" is identical regardless of how that tibble was
produced, so it lives here once rather than being duplicated per
branch.
}
\keyword{internal}
