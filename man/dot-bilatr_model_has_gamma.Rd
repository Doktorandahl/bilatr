% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_registry.R
\name{.bilatr_model_has_gamma}
\alias{.bilatr_model_has_gamma}
\title{Does a registered Stan model declare a country-level \code{gamma} offset?}
\usage{
.bilatr_model_has_gamma(stan_model)
}
\arguments{
\item{stan_model}{Name registered in \code{.bilatr_stan_models}; see
\code{\link[=.canonical_stan_model]{.canonical_stan_model()}}.}
}
\value{
\code{TRUE}/\code{FALSE}.
}
\description{
The single source of truth for whether a \code{stan_model} needs \code{gamma}-
aware behaviour downstream (0.7.0+): \code{\link[=check_compositional_residuals]{check_compositional_residuals()}}
and \code{\link[=icc_curves]{icc_curves()}} both branch on this, via the registry rather than a
user-facing flag, so a future \code{gamma}-bearing variant only needs an
entry here, not a change at every call site. \code{stable_gamma} is
currently the only one; written as an explicit set (not, say, a
\code{has_gamma} field on \code{.bilatr_stan_models} entries) because only one
entry needs it so far and this keeps the registry's existing shape
unchanged for every other model.
}
\keyword{internal}
