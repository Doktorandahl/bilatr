% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orient.R
\name{bilatr_orient}
\alias{bilatr_orient}
\title{Reorient posterior draws so \code{alpha[1]} has the canonical (positive) sign}
\usage{
bilatr_orient(draws, stan_model, variables = NULL)
}
\arguments{
\item{draws}{A \code{posterior} draws object (e.g. from \code{fit$draws()}) that
includes \code{"alpha[1]"} -- required to determine orientation even if
\code{"alpha[1]"} itself is not requested via \code{variables}.}

\item{stan_model}{Name registered in \code{.bilatr_stan_models}, or a
recognized pre-0.4.0 alias (see \code{\link[=.canonical_stan_model]{.canonical_stan_model()}}). Only
\code{"stable"}/\code{"ou"} have a reflection symmetry to correct (see
\code{\link[=.bilatr_flip_variables]{.bilatr_flip_variables()}}); for any other registered name, \code{draws}
is returned unmodified (restricted to \code{variables}, if supplied),
since there is nothing to fix. An unrecognized, non-alias name
errors instead.}

\item{variables}{Which variables to return. Defaults to every variable
\code{\link[=.bilatr_flip_variables]{.bilatr_flip_variables()}} lists for \code{stan_model}; pass a subset
(e.g. \code{"theta"}) when only that one is needed downstream.}
}
\value{
A \code{posterior::draws_df} restricted to \code{variables}, with every
variable in \code{intersect(variables, .bilatr_flip_variables(stan_model))}
negated if the posterior median of \code{alpha[1]} in \code{draws} was
negative.
}
\description{
\code{mu_intercept}, \code{phi}, and every scale/dispersion/ratio quantity
(\code{sigma_theta0}/\code{sigma_mu}, \code{process_noise}, \code{sd_stat}, \code{rho},
\verb{mu_log_*}, \verb{sigma_log_*}, \code{within_between_ratio}) are always left
untouched: \verb{alpha .* theta} is invariant under the joint negation, so
\code{mu_intercept} doesn't need to flip, and every scale/dispersion/ratio
quantity is a positive quantity, not a location, so none of them need
to either.
}
\keyword{internal}
