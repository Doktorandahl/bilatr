% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_ingestion.R
\name{download_gdelt_raw_zip}
\alias{download_gdelt_raw_zip}
\title{Download a raw GDELT export zip}
\usage{
download_gdelt_raw_zip(
  date,
  local_folder = "data/gdelt_raw",
  type = c("daily", "monthly", "yearly")
)
}
\arguments{
\item{date}{A date (or date-like string, \code{"YYYY-MM-DD"}) identifying
the period to download.}

\item{local_folder}{Directory to save the zip file into. Must already
exist.}

\item{type}{One of \code{"daily"}, \code{"monthly"}, or \code{"yearly"}.}
}
\value{
Invisibly, the path to the downloaded zip file, or \code{NULL}
(with a warning) if GDELT has no export for \code{date}.
}
\description{
Downloads a single daily, monthly, or yearly GDELT events export to
\code{local_folder}, without unzipping or processing it. Intended to be
called once per date/period; use \code{\link[furrr:future_map]{furrr::future_walk()}} (after setting
a parallel \code{\link[future:plan]{future::plan()}}) to download many periods concurrently,
since each download is independent I/O-bound work.
}
\examples{
\dontrun{
dir.create("data/gdelt_raw", recursive = TRUE, showWarnings = FALSE)
download_gdelt_raw_zip("2020-01-01", "data/gdelt_raw", type = "daily")

# many dates in parallel
future::plan(future::multisession, workers = 8)
furrr::future_walk(
  as.character(seq(as.Date("2020-01-01"), as.Date("2020-01-31"), by = "day")),
  download_gdelt_raw_zip, local_folder = "data/gdelt_raw"
)
}
}
