% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_moments.R
\name{alpha_prior_moments}
\alias{alpha_prior_moments}
\title{Prior mean/sd of \code{alpha}, per action index}
\usage{
alpha_prior_moments(A, n_sim = 2e+06, fold = TRUE, seed = NULL)
}
\arguments{
\item{A}{Number of action classes.}

\item{n_sim}{Number of Monte Carlo draws.}

\item{fold}{If \code{TRUE} (default), apply the same sign fold \code{stable}/\code{ou}
apply to \code{alpha} (see \code{orientation_sign()}); if \code{FALSE}, return the
unfolded (exchangeable) prior.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A tibble with columns \code{action_index}, \code{prior_mean}, \code{prior_sd}.
}
\description{
\code{alpha} is not exchangeable across action classes even though its prior
is: \code{orientation_sign()}'s fold breaks the symmetry for the reference
class specifically (\code{action_index == 1}), since it is defined by the
sign of that class's own raw coordinate. \code{E[alpha_k^2] = 1} exactly for
every \code{k} and any \code{A} (the fold does not change any coordinate's square),
but folding a sum-to-zero vector by its first element's sign gives that
element a materially different mean/sd than the rest -- e.g. at \code{A = 10},
\code{alpha[1]} has prior mean 0.820 and sd 0.572 while the other classes have
mean -0.091 (\verb{= -E[alpha[1]] / (A - 1)}, an identity that holds in every
single draw, not just on average, since \code{alpha} sums to exactly 0 both
before and after folding) and sd 0.996. Treating the prior as \code{N(0, 1)}
for \code{k = 1} gets both the contraction and z-score materially wrong; use
this function's output instead wherever a per-class prior sd/mean is
needed (see \code{\link[=diagnose_category_merges]{diagnose_category_merges()}}).
}
\details{
Computed by Monte Carlo (see \code{\link[=.simulate_alpha_prior]{.simulate_alpha_prior()}}) rather than a
closed form, so it stays correct if the underlying Stan construction
ever changes; results are memoised per \verb{(A, n_sim, fold, seed)}.
}
\examples{
alpha_prior_moments(4)
}
