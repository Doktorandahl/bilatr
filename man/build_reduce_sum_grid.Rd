% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{build_reduce_sum_grid}
\alias{build_reduce_sum_grid}
\title{Build a (cores x grainsize x replicate) benchmark grid}
\usage{
build_reduce_sum_grid(
  n_dyads,
  thread_levels = c(4, 8, 12, 16, 24, 32, 48),
  grainsize_levels = NULL,
  replicates = 3L,
  grainsize_span = c(1/200, 1/6),
  n_grainsize = 9L
)
}
\arguments{
\item{n_dyads}{Integer, number of dyads (\code{D}) in the assembled Stan
data for the scenario being benchmarked.}

\item{thread_levels}{Integer vector of \code{threads_per_chain} values to
test. Default \code{c(4, 8, 12, 16, 24, 32, 48)}.}

\item{grainsize_levels}{Optional integer vector of \code{reduce_sum}
grainsizes. If \code{NULL} (default), generated from \code{n_dyads} and
\code{grainsize_span} / \code{n_grainsize}.}

\item{replicates}{Number of repeat runs per (cores, grainsize) cell,
to average over cluster wall-clock noise. Default 3.}

\item{grainsize_span}{Length-2 numeric, the low/high multipliers of
\code{n_dyads} bounding the generated grainsize sequence. Ignored if
\code{grainsize_levels} is supplied.}

\item{n_grainsize}{Number of grainsize levels to generate. Ignored if
\code{grainsize_levels} is supplied.}
}
\value{
A tibble with one row per job: \code{job_id}, \code{cores}, \code{grainsize},
\code{replicate}, and the derived \code{n_chunks} (\code{ceiling(n_dyads / grainsize)}) and \code{chunks_per_core}. Rows are ordered by \code{cores},
then \code{grainsize}, then \code{replicate}, so a SLURM array filtered to one
\code{cores} level indexes rows \verb{1..(n_grainsize * replicates)} in that
order.
}
\description{
Constructs the parameter grid for a \code{reduce_sum} threading sweep on a
panel of \code{n_dyads} dyads. The Stan model slices \code{dyad_seq} (length
\code{D == n_dyads}) inside \code{reduce_sum}, so \code{grainsize} is a count of
dyads per task and the useful range scales with \code{n_dyads}: when
\code{grainsize_levels} is not supplied it is generated as a geometric
sequence spanning \code{grainsize_span * n_dyads} (default: \code{n_dyads / 200}
up to \code{n_dyads / 6}), which brackets the previously Pareto-optimal
grainsize of ~600 for \code{n_dyads} ~ 16750.
}
\keyword{internal}
