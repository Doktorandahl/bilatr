% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{summarise_benchmark}
\alias{summarise_benchmark}
\title{Summarise raw per-job benchmark results to one row per grid cell}
\usage{
summarise_benchmark(results, group_vars = c("scenario", "cores", "grainsize"))
}
\arguments{
\item{results}{A data frame of per-job benchmark rows, as written by
\code{runscripts/benchmark/bench_worker.R} (one row per job). Must contain
the grouping columns and the timing columns \code{sampling_sec},
\code{iter_per_sec}, \code{core_sec_per_1k_iter}, and optionally
\code{reduce_sum_wall_sec}.}

\item{group_vars}{Columns identifying a grid cell. Defaults to
\code{c("scenario", "cores", "grainsize")}; any not present in \code{results}
are dropped.}
}
\value{
A tibble with one row per cell: replicate count and the median
(plus MAD for the headline metric) of each timing column.
}
\description{
Summarise raw per-job benchmark results to one row per grid cell
}
\keyword{internal}
