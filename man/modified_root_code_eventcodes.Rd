% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cameo_recode.R
\name{modified_root_code_eventcodes}
\alias{modified_root_code_eventcodes}
\title{Underlying CAMEO event codes for a \code{ModifiedRootCode} value}
\usage{
modified_root_code_eventcodes(class)
}
\arguments{
\item{class}{Integer vector of \code{ModifiedRootCode} values, as returned
by \code{\link[=assign_modified_root_code]{assign_modified_root_code()}}.}
}
\value{
Character vector of comma-separated CAMEO root/event codes,
\code{NA} for values outside 1-18.
}
\description{
For most \code{ModifiedRootCode} classes this is just the single two-digit
CAMEO root code the class was built from. Where a class merges several
roots (e.g. "Investigate or demand"), splits a root into finer
sub-codes (e.g. "Consult: meet, discuss, or visit"), or relocates an
individual code out of its root (e.g. "Reject", which picks up \code{016}
from root 01), this lists every code that feeds into it, comma-
separated. Root \code{01}'s own entry notes the codes carved out of it
(\code{016}/\code{018}/\code{019}) as an exception, since the rest of root \code{01} stays
in this class. The bare two-digit root \code{04} is omitted from the
"Consult, unspecified" entry (\code{"040"} only, not \code{"04, 040"}), since
root \code{04} itself splits across several \code{ModifiedRootCode} classes and
so is blanked at the top level; see \link{cameo_lookup}'s docs.
}
\keyword{internal}
