% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_csv_read.R
\name{.strip_stan_csv_comments}
\alias{.strip_stan_csv_comments}
\title{Strip comment lines from a raw CmdStan CSV, streaming, to a real file}
\usage{
.strip_stan_csv_comments(csv_file, out_file, chunk_size = 200000L)
}
\arguments{
\item{csv_file}{A single raw CmdStan CSV file path.}

\item{out_file}{Destination path for the comment-stripped copy.}
}
\value{
\code{out_file}, invisibly.
}
\description{
CmdStan's own CSV format intersperses \verb{#}-prefixed comment lines (a
config block up front, an "Adaptation terminated" marker between
warmup and post-warmup rows, and a timing footer) with the actual
header/data rows. \code{\link[cmdstanr:read_cmdstan_csv]{cmdstanr::read_cmdstan_csv()}} strips these via
\code{data.table::fread(cmd = "grep -v '^#' <file>")}; per \code{?data.table::fread},
a \verb{cmd=}/piped input is first run to completion with its \emph{entire}
output written to a fresh file in \code{tempdir()}, and only then read "as
normal" -- so every single call, however few \code{variables} it requests,
pays for a full near-complete copy of that one chain's raw CSV (often
several GB at production scale) landing somewhere under \code{tempdir()}.
\link{.chunked_summarise_csv} calls this once per chunk, so a Tier 3 sweep
with hundreds of chunks repeats that copy hundreds of times per chain
-- and if \code{tempdir()}/\verb{$TMPDIR} happens to be a RAM-backed \code{tmpfs} (a
common per-node HPC/SLURM default), each copy is a direct hit against
the job's memory allocation, independent of \code{max_memory_mb}/
\code{chunk_size}, which is what actually exhausts memory on jobs like
this one however tightly \code{max_memory_mb} is set.
}
\details{
This does the same comment-stripping exactly once per chain file (see
\link{.prepare_fast_csv_read}, which every CSV-path entry point calls a
single time up front, before any chunking), streaming line-by-line via
\code{\link[readr:read_lines_chunked]{readr::read_lines_chunked()}} rather than shelling out to \code{grep}, so
peak memory here is bounded by \code{chunk_size} lines rather than the
whole file, and it depends on no external binary (portable to
Windows/any system with just R installed -- no \code{grep}/\code{grep.exe} PATH
assumptions). The output is written directly to \code{out_file}, a real
path the caller controls (see \code{scratch_dir} in
\link{.prepare_fast_csv_read}), never through \code{tempdir()}.
}
\keyword{internal}
