% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\docType{data}
\name{.BILATR_CHUNK_CORES_STEP_FACTOR}
\alias{.BILATR_CHUNK_CORES_STEP_FACTOR}
\title{Memory-model constant: the one-time cost of \code{posterior:: summarise_draws()} forking at all (\code{.cores > 1})}
\format{
An object of class \code{numeric} of length 1.
}
\usage{
.BILATR_CHUNK_CORES_STEP_FACTOR
}
\description{
Applied once, whenever \code{n_cores > 1}, together with
\link{.BILATR_CHUNK_CORES_PER_CORE_FACTOR} -- see
\code{\link[=.bilatr_chunk_overhead_multiplier]{.bilatr_chunk_overhead_multiplier()}}. Measured, not assumed: see
that constant's docs for why this and the per-core term replace the
old single flat \code{CORES_FACTOR} (which this package's own pinned
tests, before this measurement, asserted was identical from
\code{n_cores = 2} through \code{24} -- that assumption was never measured
against a real fork and was wrong).
}
\keyword{internal}
