% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_moments.R
\name{.simulate_alpha_prior}
\alias{.simulate_alpha_prior}
\title{Simulate draws from \code{alpha}'s implied prior}
\usage{
.simulate_alpha_prior(A, n_sim = 2e+06, fold = TRUE, seed = NULL)
}
\arguments{
\item{A}{Number of action classes.}

\item{n_sim}{Number of Monte Carlo draws.}

\item{fold}{If \code{TRUE} (default), apply the same sign fold \code{stable}/\code{ou}
apply to \code{alpha} (see \code{orientation_sign()}); if \code{FALSE}, return the
unfolded (exchangeable) prior.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
An \verb{n_sim x A} numeric matrix of simulated \code{alpha} draws.
}
\description{
Mirrors \code{inst/stan/bilatr_alphanorm.stan}'s \verb{transformed parameters}
construction of \code{alpha} exactly, so the two cannot silently drift apart:
\code{alpha_raw ~ std_normal()} on a \code{sum_to_zero_vector[A]} is equivalent in
distribution to an iid-normal draw with its row mean subtracted (both are
the unique zero-mean isotropic Gaussian confined to the sum-zero
hyperplane); \code{alpha} is then \code{alpha_raw} scaled to RMS 1
(\code{sqrt(A / dot_self(alpha_raw))}, see \code{orientation_sign()} and the
\code{alpha} assignment in that file's \verb{transformed parameters} block), and
folded by the sign of the first element if \code{fold = TRUE} (matching
\code{orientation_sign(alpha_raw)}). If that Stan construction ever changes,
this function must change with it.
}
\keyword{internal}
