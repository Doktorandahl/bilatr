% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.flag_worse_than_expected}
\alias{.flag_worse_than_expected}
\title{Flag diagnostics that are worse than a smooth sparsity trend predicts}
\usage{
.flag_worse_than_expected(n_dt, value, n_bins = 6L)
}
\arguments{
\item{n_dt}{Numeric vector of per-dyad observation counts.}

\item{value}{Numeric vector (same length/order as \code{n_dt}) of the
diagnostic to flag (e.g. ESS_bulk); lower is assumed worse.}

\item{n_bins}{Target number of quantile bins; automatically reduced
for small \code{n_dt}.}
}
\value{
Logical vector, same length as \code{n_dt}/\code{value}: \code{TRUE} where the
value is worse than its bracket's expectation. \code{NA} where \code{value} is
\code{NA}.
}
\description{
Bins dyads into \code{n_dt} quantile buckets and flags a dyad's diagnostic
value as "worse than expected for its sparsity bracket" when it falls
below \code{median - 1.5 * IQR} of its own bucket. This is deliberately a
within-bracket outlier rule rather than a smoother (loess/rank
regression): it needs no bandwidth/shape assumptions, degrades
gracefully with few dyads (bucket count shrinks automatically), and
the "1.5 * IQR" rule is the standard Tukey outlier convention, so a
flagged dyad is an outlier \emph{relative to equally-sparse peers}, not
merely a sparse dyad.
}
\keyword{internal}
