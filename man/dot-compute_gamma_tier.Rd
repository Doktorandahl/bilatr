% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.compute_gamma_tier}
\alias{.compute_gamma_tier}
\title{Compute the \code{gamma} (country-level offset) diagnostics tibble}
\usage{
.compute_gamma_tier(summ, rhat_threshold, ess_threshold, country_codes = NULL)
}
\arguments{
\item{summ}{A tibble as produced by \code{\link[posterior:draws_summary]{posterior::summarise_draws()}},
left-joined with \link{.classify_bilatr_tier}'s \code{tier} column.}

\item{country_codes}{Optional character vector (in \code{country_index}
order, e.g. \code{stan_data}'s \code{"country_codes"} attribute) for labelling
\code{country_index}. \code{NULL} (e.g. plain \code{\link[=diagnose_convergence]{diagnose_convergence()}}, which
has no \code{stan_data}) leaves the table with \code{country_index} only.}
}
\value{
A tibble: \code{variable}, \code{action_index}, \code{country_index} (and
\code{country_code} if \code{country_codes} is supplied), \code{rhat}, \code{ess_bulk},
\code{ess_tail}, \code{flagged}.
}
\description{
Split out of Tier 1 (0.7.1) so that a routine number of flagged
\code{gamma} elements -- dozens to hundreds at production scale, just from
\code{ess_threshold} against ~4,000 draws -- doesn't flood
\code{\link[=print.bilatr_diagnostics]{print.bilatr_diagnostics()}}'s Tier 1 block or dominate
\code{n_tier1_flagged}. \code{country_code}/\code{action_index} are parsed directly
from the \code{gamma[k,c]} variable name (self-contained; does not depend
on \code{\link[=.classify_bilatr_tier]{.classify_bilatr_tier()}} exposing raw bracket indices), the same
pattern \code{\link[=extract_gamma]{extract_gamma()}} uses.
}
\keyword{internal}
