% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{pareto_front}
\alias{pareto_front}
\title{Flag the Pareto-optimal cells of a benchmark summary}
\usage{
pareto_front(df, throughput, cost, by = NULL)
}
\arguments{
\item{df}{A data frame (e.g. from \code{\link[=summarise_benchmark]{summarise_benchmark()}}).}

\item{throughput}{Name of the throughput column (higher is better),
e.g. \code{"iter_per_sec_median"}.}

\item{cost}{Name of the cost column (lower is better), e.g.
\code{"core_sec_per_1k_iter_median"}.}

\item{by}{Optional character vector of grouping columns; the frontier
is computed separately within each group.}
}
\value{
\code{df} with an added logical \code{pareto} column.
}
\description{
Marks each row as on the Pareto frontier of (higher \code{throughput},
lower \code{cost}) or dominated by another row. With \code{by} supplied, the
frontier is computed within each group (e.g. per scenario).
}
\keyword{internal}
