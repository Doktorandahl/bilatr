% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category_merges.R
\name{diagnose_category_merges}
\alias{diagnose_category_merges}
\title{Which event categories can be merged, and what it would cost}
\usage{
diagnose_category_merges(
  fit,
  stan_data,
  event_classes = NULL,
  class_label_fn = NULL,
  shares = NULL,
  phi = NULL,
  probs = c(0.05, 0.5, 0.95)
)
}
\arguments{
\item{fit}{A \code{CmdStanMCMC}-like fit object, or a character vector of
CmdStan CSV file paths (see \code{\link[=.get_draws]{.get_draws()}}).}

\item{stan_data}{The Stan data list used to produce \code{fit}, as returned
by \code{\link[=assemble_stan_data]{assemble_stan_data()}} (used for the default empirical \code{shares}
and the \code{effective_info} figure's mean event count).}

\item{event_classes}{Character vector of event-class labels, length
\code{A}, in the same order as \code{stan_data}'s \code{"event_classes"} attribute.
Defaults to that attribute if present, else \verb{"1", "2", ...}.}

\item{class_label_fn}{Optional function mapping an integer
\code{action_index} vector to pretty labels (e.g. \code{bilatr::: eventrootcode3_name}), matching the scheme \code{event_classes} was built
under. There is no scheme registry in this package (each CAMEO
regrouping has its own standalone namer in \code{R/cameo_recode.R}), so
the caller supplies the right one for their scheme, exactly as the
\verb{runscripts/} do. Defaults to \code{NULL} (raw \code{event_classes} used as
the label too).}

\item{shares}{Reference category shares: \code{NULL} (default) uses the
empirical shares from \code{stan_data$Y}, summed over dyads/periods and
normalised (a single, FIXED reference point); \code{"theta0"} evaluates at
\code{softmax(-mu_intercept)} instead, per draw; or a user-supplied
numeric vector of length \code{A} (fixed). \code{pi} is a function of \code{theta},
so any single share vector makes this a \emph{local} measure at one point
on the scale -- a category that discriminates only at extreme
\code{theta} will be undervalued regardless of which reference is used.
The reference actually used is recorded in \verb{$summary$shares_used}
and printed.}

\item{phi}{Dirichlet-multinomial dispersion used only for the
\code{effective_info} summary figure (the merge ranking is exactly
invariant to it): \code{NULL} (default) uses, per draw, the posterior
median of \code{phi} across dyads; or a user-supplied scalar/per-dyad
vector (per-dyad vectors are averaged, since this feeds one
representative figure, not a per-dyad-period computation).}

\item{probs}{Posterior interval bounds to report alongside the mean
(first and last are used as the interval; a third, e.g. the median,
is ignored for \code{pairwise}/\code{ladder} but not \code{categories}).}
}
\value{
A \code{bilatr_category_merges} object: \code{categories} (one row per
class: \code{action_index}, \code{event_class}, \code{class_label}, \code{post_mean},
\code{post_sd}, \code{share}, \code{info_share}, \code{contraction}, \code{z_score},
\code{precision_gain}), \code{pairwise} (every pair: indices, labels, \code{d_alpha},
\code{pct_info_lost} mean/interval), \code{ladder} (the greedy agglomerative
path from \code{A} down to 2 -- \strong{read this one}: it gives the whole
curve so a knee can be picked rather than pricing one grouping at a
time; greedy is not guaranteed globally optimal for a fixed target
\code{A}, standard for agglomerative methods), and \code{summary} (scalars for
\code{print()}).
}
\description{
Two categories are redundant \emph{for the latent scale} exactly when their
discriminations (\code{alpha}) are equal: the Fisher information about
\code{theta} from one dyad-period's multinomial is \code{n * Var_pi(alpha)}, with
\verb{Var_pi(alpha) = sum_k pi_k (alpha_k - alpha_bar)^2} and \verb{alpha_bar = sum_k pi_k alpha_k}. Merging categories \code{j} and \code{k} gives the merged
category the share-weighted mean discrimination, and the drop in
\code{Var_pi(alpha)} is exactly
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{loss(j, k) = pi_j * pi_k * (alpha_j - alpha_k)^2 / (pi_j + pi_k)
}\if{html}{\out{</div>}}

Derivation: before merging, \code{j}/\code{k} contribute `pi_j(alpha_j-alpha_bar)^2
\itemize{
\item pi_k(alpha_k-alpha_bar)^2\code{to}Var_pi(alpha)\verb{; merging replaces this with }pi_jk(alpha_jk-alpha_bar)^2\code{where}pi_jk = pi_j+pi_k\code{and}alpha_jk\verb{is the share-weighted mean (chosen so}pi_jk\emph{alpha_jk =
pi_j}alpha_j + pi_k\emph{alpha_k\verb{, which leaves the OVERALL }alpha_bar\verb{unchanged by the merge). Expanding the difference, every}alpha_bar\verb{term cancels and what remains is exactly}pi_j}pi_k*(alpha_j-alpha_k)^2
/ (pi_j+pi_k)\verb{. The general-}S\verb{formula is the same algebra applied to a group. **This is exactly Ward's linkage** on the one-dimensional}alpha\verb{values with weights}pi\verb{ (}stats::hclust(method = "ward.D")`
reproduces the greedy ladder below, used as a test, not the
implementation).
}

Losses are reported as a \strong{fraction of \code{Var_pi(alpha)}}, so they read
directly as "merging these costs x\% of the information about theta."
The Dirichlet-multinomial overdispersion correction (\code{phi}) scales
every term equally, so it does not affect this ranking at all -- see
\code{summary$effective_info} for the one absolute (phi-corrected) figure
this function reports, using \code{phi} and the mean per-dyad-period event
count from \code{stan_data$Y}.

\strong{\code{alpha} is a discrimination, not a severity.} A category can have a
strongly negative \code{alpha} while representing objectively severe
conduct: rare extreme acts discriminate poorly precisely because they
are rare and can occur in dyads that are not systematically hostile,
whereas pervasive low-level behavior tracks the latent state closely.
Do not read the \code{alpha} ordering as a severity ranking.

\strong{Cheapness is a price, not a recommendation.} This function reports
what merging costs in information about \code{theta}; it does not say
whether to merge, and it never emits a recommended grouping. When the
cost is negligible, the decision belongs to interpretability,
identification hygiene, and whether a category is so sparse that its
Dirichlet concentration is near zero and the cell is effectively
unconstrained.

\strong{Compute per draw, then summarise.} The loss is nonlinear in \code{alpha},
so its value at the posterior mean is not its posterior mean; every
quantity here is computed for each draw and then summarised, which
also handles \code{alpha}'s correlation structure for free (the sum-to-zero
and RMS-1 constraints make the \code{alpha_k} strongly negatively
correlated, so \code{sd(alpha_j - alpha_k)} is emphatically not
\code{sqrt(sd_j^2 + sd_k^2)}).

\strong{On a \code{stable_gamma} fit (0.7.0):} every measure here (\code{alpha},
its correlation structure, the reference \code{shares}) is evaluated
exactly as for any other model -- \code{gamma}/\code{g_d} never enter this
function's computation at all. For \code{stable_gamma}, that means the
reference share vector implicitly used (whether the empirical default,
\code{"theta0"}, or a user-supplied \code{shares}) is the \code{gamma = 0} baseline,
the same baseline \code{\link[=icc_curves]{icc_curves()}}'s \code{country = NULL} default uses --
not any particular country's actual share vector. Left this way
deliberately (unchanged computation, documented meaning): a
country-specific merge analysis is a coherent further extension but is
not implemented here.
}
