% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.chunked_summarise_csv_with_orientation}
\alias{.chunked_summarise_csv_with_orientation}
\title{Chunked-summarise a Tier 3 (or theta-only) variable set, determining
sign orientation from the first chunk instead of a separate \code{alpha[1]}
read}
\usage{
.chunked_summarise_csv_with_orientation(
  prepared,
  variables,
  chunk_size,
  n_cores,
  flip_vars
)
}
\arguments{
\item{prepared}{Output of \link{.prepare_fast_csv_read}.}

\item{variables}{Character vector of Tier 3 (or theta-only) variable
names to summarise.}

\item{chunk_size}{Variables per chunk.}

\item{n_cores}{See \code{\link[=.chunked_summarise_csv]{.chunked_summarise_csv()}}.}

\item{flip_vars}{Output of \code{\link[=.bilatr_flip_variables]{.bilatr_flip_variables()}} for the model
being read.}
}
\value{
A list with \code{summ} (the row-bound summary tibble, same shape
\code{\link[=.chunked_summarise_csv]{.chunked_summarise_csv()}} returns) and \code{flip} (logical, the
orientation decision -- for a caller that also needs to flip a
Tier 1/2 read using the same decision).
}
\description{
Shared by \code{\link[=diagnose_and_extract_bilatr]{diagnose_and_extract_bilatr()}} and \code{\link[=extract_theta]{extract_theta()}}'s CSV
branches for the case where no Tier 1/2 read is already happening to
piggyback \code{alpha[1]} onto (i.e. \code{tiers} excludes both 1 and 2, or
\code{extract_theta()}, which never reads anything but \code{theta}). If
\code{flip_vars} is empty (the model has no reflection symmetry) or
\code{variables} is empty, this is exactly \code{\link[=.chunked_summarise_csv]{.chunked_summarise_csv()}} with
\code{flip = FALSE} -- no \code{alpha[1]} read at all. Otherwise, \code{"alpha[1]"}
is prepended to the FIRST chunk only (unless it's already in it),
read once unflipped; its posterior median decides \code{flip}; that
chunk's \code{flip_vars}-matching columns (see
\code{\link[=.bilatr_match_draws_columns]{.bilatr_match_draws_columns()}}) are negated directly on the
\code{draws_array} (mirroring \code{\link[=.read_and_orient_draws_summary]{.read_and_orient_draws_summary()}}'s
array-native flip -- no \code{draws_df} round-trip, which would force
\code{summarise_draws()} to rebuild a \code{draws_array} internally at the cost
of a second full copy) and, if \code{"alpha[1]"} was only added for this
orientation check, its row is dropped from the first chunk's summary
afterward, the same way \code{\link[=.read_and_orient_draws_summary]{.read_and_orient_draws_summary()}} does it;
every remaining chunk is then read via \code{\link[=.chunked_summarise_csv]{.chunked_summarise_csv()}}
with \code{flip} already known. A full sweep still costs exactly one read
per chunk, with no extra pass just to check \code{alpha[1]}'s sign.
}
\keyword{internal}
