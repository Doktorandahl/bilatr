% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_convergence.R
\name{.stan_csv_dims}
\alias{.stan_csv_dims}
\title{Cheaply get per-chain draw count and chain count from CmdStan CSVs}
\usage{
.stan_csv_dims(csv_files)
}
\arguments{
\item{csv_files}{Character vector of CmdStan CSV file paths.}
}
\value{
A list with \code{n_draws} and \code{n_chains}.
}
\description{
\code{n_draws} comes from the same header-only metadata read as
\link{.stan_csv_variable_names}; \code{n_chains} is simply \code{length(csv_files)}
(one file per chain, per this package's own SLURM submission
convention -- see \code{runscripts/submit_bilatr_runs.R}), not the
\code{num_chains} metadata field, which records only the single chain each
individual CSV file's own run was configured for.
}
\keyword{internal}
