% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.legacy_alpha_raw_sum0_init}
\alias{.legacy_alpha_raw_sum0_init}
\title{A sign-biased sum-to-zero init draw for the legacy \code{stable_soft_anchor}/
\code{ou_soft_anchor} programs' \code{alpha_raw}}
\usage{
.legacy_alpha_raw_sum0_init(A)
}
\arguments{
\item{A}{Number of action types.}
}
\value{
A length-\code{A} numeric vector summing to exactly 0, with its
first element positive.
}
\description{
Legacy-only since 0.4.2: \code{stable}/\code{ou} build \code{alpha_raw} from a
\code{real<lower=0> alpha_raw_1} and no longer need this (see each
\code{.stan} file's header, \verb{IDENTIFICATION: alpha[1] > 0 BY CONSTRUCTION}). \code{stable_soft_anchor}/\code{ou_soft_anchor} still declare
\code{alpha_raw} as a free \code{sum_to_zero_vector[A]}, normalized by
\code{sqrt(A / dot_self(alpha_raw))}, so an all-zero init (otherwise the
natural default) would divide by zero; this draws a random vector
instead, centered to sum to exactly 0 (required by
\code{sum_to_zero_vector}'s constrained representation) and away from the
\code{dot_self(alpha_raw) == 0} degeneracy.
}
\details{
Those two programs also anchor \code{alpha[1]}'s sign with a soft penalty
in the model block (breaking an exact reflection symmetry: alpha,
theta -> -alpha, -theta leaves the likelihood, every prior, and the
\code{sum_to_zero_vector} Jacobian unchanged), but that penalty only makes
the TARGET correctly specified -- it does not, by itself, make a
chain visit the \code{alpha[1] > 0} basin, since the two basins are
separated by a likelihood barrier of thousands of nats that a chain
essentially never crosses during warmup. This negates the whole draw
whenever its first element came out negative, so chains START already
in the anchored basin instead of relying on warmup to find it. This is
a bias, not a guarantee for every init/data/seed combination -- see
\code{bilatr_orient()} (\code{R/orient.R}) for the deterministic, always-correct
fallback that relabels draws after the fact regardless of which basin
a chain actually landed in, and \code{.warn_if_wrong_basin()} below for the
automatic post-sampling check that flags it when this bias didn't work.
}
\keyword{internal}
