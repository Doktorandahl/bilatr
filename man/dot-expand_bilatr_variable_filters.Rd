% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_csv_read.R
\name{.expand_bilatr_variable_filters}
\alias{.expand_bilatr_variable_filters}
\title{Expand variable filters to concrete variable names, cmdstanr-style}
\usage{
.expand_bilatr_variable_filters(available, filters)
}
\arguments{
\item{available}{Character vector of all bracket-form variable names
in the CSV header (\code{names(prepared$dot_name_map)}).}

\item{filters}{Character vector of requested variable names/base names.}
}
\value{
Character vector of concrete, available variable names, in
the order their filters were given (a base-name filter expands to
its matches in \code{available}'s order).
}
\description{
Replicates \code{cmdstanr:::matching_variables()}'s two-step lookup so
\link{.fast_read_post_warmup_draws} accepts the same \code{variables} spellings
\code{\link[cmdstanr:read_cmdstan_csv]{cmdstanr::read_cmdstan_csv()}} does: an exact match (e.g.
\code{"theta[3,12]"}, \code{"alpha[1]"}) is used as-is, and anything that
doesn't match exactly (e.g. a bare base name like \code{"alpha"} or
\code{"mu_intercept"}, as \link{.get_draws} passes for \code{\link[=extract_alpha]{extract_alpha()}}/
\code{\link[=extract_mu_intercept]{extract_mu_intercept()}}) is expanded to every available variable
whose name starts with \code{"<filter>["} -- i.e. every indexed element of
that parameter.
}
\keyword{internal}
